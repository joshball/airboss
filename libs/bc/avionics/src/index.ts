// Avionics BC -- glass-cockpit telemetry types. Phase 0 scaffold:
// types only, no runtime. The PFD demo wires PFD slider state into
// `AvionicsTelemetry` and instruments read from it.

export type { AirData, Attitude, AvionicsTelemetry, NavData } from './types';
