/**
 * study/sectional palette -- light.
 *
 * Sectional is the reading-column study theme. It inherits every
 * concrete value from airboss/default today; the file exists so
 * future divergence has a landing zone.
 */

import type { Palette } from '../../contract';
import { palette as base } from '../../core/defaults/airboss-default/palette.light';

export const palette: Palette = base;
