/**
 * TODO(Package #6): replace this stub with a real dark palette.
 *
 * Currently identical to the sectional light palette so `data-appearance="dark"`
 * resolves without a FOUC flash, even though the actual dark treatment is
 * deferred to package #6.
 */

import { sectionalLightPalette } from './palette.light';

export const sectionalDarkPalette = sectionalLightPalette;
