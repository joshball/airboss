/**
 * TODO(Package #6): replace this stub with a real dark palette.
 *
 * Same OKLCH values as flightdeck light so `data-appearance="dark"` is
 * a valid selector; package #6 replaces with the tuned dark swatches.
 */

import { flightdeckLightPalette } from './palette.light';

export const flightdeckDarkPalette = flightdeckLightPalette;
