/**
 * TODO(Package #6): replace this stub with a real dark palette.
 *
 * Ships as a copy of the light values for now so `data-appearance="dark"`
 * is a valid selector but doesn't visually diverge yet. Package #6
 * ("appearance-dark-mode") substitutes tuned OKLCH swatches, cookie toggle,
 * and dark-surface contrast tests. Do not hand-migrate this file in the
 * interim -- it's tracked as a known stub.
 */

import { airbossDefaultLightPalette } from './palette.light';

export const airbossDefaultDarkPalette = airbossDefaultLightPalette;
