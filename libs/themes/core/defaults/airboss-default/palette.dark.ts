/**
 * Airboss default palette -- DARK STUB.
 *
 * TODO(package-06): replace with a real dark palette. These values are
 * a verbatim copy of the light palette so `emitAllThemes()` has
 * something to resolve for `appearance=dark` selectors. The
 * appearance-switch plumbing (cookie, pre-hydration script,
 * `<html data-appearance>`) is exercised in package #1; real dark
 * colors land in package #6.
 */

export { palette } from './palette.light';
