.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
*.pdf
.ruff_cache/

# Compare-strategy reports are regenerated by `bun run sources extract`
# `--strategy compare`. Source-controlled inputs (prompts-out/, handbooks/,
# YAML config) plus the cached PDF reproduce them deterministically.
reports/
