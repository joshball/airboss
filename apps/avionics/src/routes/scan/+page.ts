import type { PageLoad } from './$types';

// Placeholder route. The scored scan-trainer drill is its own WP; the
// page exists today so the home grid never points at a 404.
export const load: PageLoad = () => {
	return {};
};
