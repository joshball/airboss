import type { PageLoad } from './$types';

// Phase 3 stub: the avionics home is its own page (not a redirect),
// and Phase 5 fills it with the card grid. No data needed today.
export const load: PageLoad = () => {
	return {};
};
