import type { PageLoad } from './$types';

// Placeholder route. Real MFD content lands with the MFD product WP;
// this page exists today so the home grid never points at a 404.
export const load: PageLoad = () => {
	return {};
};
