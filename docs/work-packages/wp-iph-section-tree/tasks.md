# WP-IPH -- tasks

Phased build per the parent prompt. One commit per phase.

## Phase 0 -- spec docs

- [x] Create `docs/work-packages/wp-iph-section-tree/{spec,tasks,test-plan}.md`.
- [x] Branch `feat/wp-iph-section-tree` off `origin/main`.

## Phase 1 -- YAML config

- [x] Author `scripts/sources/config/handbooks/iph.yaml` modeled after `afh.yaml`:
  - `document_slug: iph`
  - `edition: FAA-H-8083-16B`
  - `kind: handbook`
  - `subjects: [procedures, navigation, flight-instruments]`
  - `primary_cert: instrument` (sourced from the existing handbooks-extras row)
  - `whole_doc.url` + `filename`
  - `chapter_pdfs.direct_pattern: https://.../FAA-H-8083-16B_Chapter_{N}.pdf`
  - `chapter_pdfs.chapter_count: 7`
  - `chapter_pdfs.file_ordinal_offset: 0`
  - `chapter_pdfs.ancillary`: front, toc, appendix×2, glossary
  - `outline_strategy: content`
  - `section_strategy: toc`
  - `toc.page_start: 12`, `toc.page_end: 16`, `toc.pattern: dotted_leader`
  - `heading_style.verify_in_body: false` (mirrors AFH/AVWX -- TOC parse is the source of truth)
  - `prompt.chapter_text_max_chars` for prompt-fallback symmetry
  - empty `errata: []` and `dismissed_errata: []`

## Phase 2 -- run download + extract

- [x] `bun run sources download --only handbooks` (caches the chapter PDFs + ancillaries; the existing `--include-handbooks-extras` flag becomes irrelevant for IPH).
- [x] `bun run sources extract handbooks iph --edition FAA-H-8083-16B`.
- [x] Verify the produced manifest at `handbooks/iph/FAA-H-8083-16B/manifest.json` carries `sections[]` with chapter / section / subsection rows.

## Phase 3 -- remove from handbooks-extras

- [x] Delete the `faa-h-8083-16` row from `scripts/sources/config/handbooks-extras.yaml`.
- [x] Delete the `'faa-h-8083-16'` entry from `DOC_ID_TO_FRIENDLY` in `libs/sources/src/handbooks-extras/ingest.ts`.
- [x] Delete the `iph: { short, formal }` entry from `FRIENDLY_DISPLAY` in the same file.
- [x] Delete `handbooks/iph/FAA-H-8083-16B/iph-FAA-H-8083-16B.md` (now superseded by the per-section markdown tree).
- [x] Update `libs/sources/src/handbooks-extras/ingest.test.ts`:
  - `DOC_ID_TO_FRIENDLY` coverage list: drop `'faa-h-8083-16'`.
  - Live-cache smoke test: `expect(report.ingested).toBe(5)` → 4; remove `'iph'` from the per-slug derivative-existence loop.
- [x] Update `handbooks/handbooks-extras-index.json`: drop the IPH entry. (The file is regenerated by the ingest, but the canonical version stays in the repo; we adjust by re-running register.)

## Phase 4 -- verify

- [x] `bun run check` -- 0 errors / 0 warnings.
- [x] `bunx vitest run libs/sources` -- handbook unit tests pass.
- [x] `bun run sources verify-urls` -- IPH URLs resolve.

## Phase 5 -- tests + docs

- [x] Add a manifest-validation unit test under `libs/sources/src/handbooks/` that loads the IPH manifest and asserts:
  - `sections.length` matches the expected chapter count + first-level section TOC entries.
  - `kind === 'handbook'` (not `whole-doc`).
  - `body_path` is absent from the manifest top level.
- [x] Update `docs/platform/REFERENCES.md`: IPH row → "✅ readable, section-tree", note chapter / section counts. Remove the "promotion WP pending" note from the IPH and the row in the "Whole-doc handbooks → promote to section-tree" table.
- [x] Mark this WP `status: shipped` in spec.md (after PR merge).

## Coordination

mtn-tips, RMH, AIH all running in parallel. They modify the same
`handbooks-extras.yaml` and `DOC_ID_TO_FRIENDLY` map, but they own
different `doc_id`s so changes coexist; whichever PR lands first wins,
the others rebase.
