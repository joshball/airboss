---
title: 'TODO: 2026-05-02 -- hangar-invite-flow build'
type: todo
date: 2026-05-02
---

Build the hangar-invite-flow work package per [tasks.md](../../work-packages/hangar-invite-flow/tasks.md).

## Phases

- [x] Phase 1 -- Constants + types
- [x] Phase 2 -- Drizzle schema + migration
- [x] Phase 3 -- Zod schemas
- [x] Phase 4 -- BC reads + writes + tests
- [x] Phase 5 -- Email template
- [x] Phase 6 -- Hangar UI (list + detail)
- [x] Phase 7 -- Study accept route + hooks exemption
- [x] Phase 8 -- Help pages
- [x] Phase 9 -- Doc updates + final review + PR

## Shipped

PR #444 squash-merged to main as `71f65f33`. 23/23 BC vitest cases passing. `bun run db reset --force` confirmed schema applies cleanly: `hangar.invitation` materialized with all 13 columns + 4 indexes + 3 FKs, and the `audit_log_target_type_check` constraint now allows `'hangar.invitation'`.

Three rebases through 16 PRs that landed mid-build (#447, #448 absorbed via #450, #449 the lib UI extract, #459 perf cluster, #460-#466 review-closure work, #467 chunk-6 closures). Each rebase resolved deterministically -- the only judgement call was dropping the duplicate "Send an invite" Banner on `/users` since the `invitations-link` action button in the page header already serves that nudge.

## Bonus PRs landed in this session (drizzle baseline reset)

- **PR #445** -- collapsed 11 broken migrations into a single fresh `0000_initial.sql`. Four prior PRs had each added a `.sql` file without journaling it, plus there was a `0007_*` prefix collision.
- **PR #450** -- absorbed #448's `0010_hangar_job_log_seq_unique` (which merged ~60 seconds after #445 with the same anti-pattern) and added `drizzle/README.md` documenting the `push`-only convention so the next agent sees it at the point of edit.

## User actions remaining

- Manual smoke per [test-plan.md](../../work-packages/hangar-invite-flow/test-plan.md) (HIF-1 through HIF-91).
- Flip `review_status: pending` -> `done` in `spec.md` + `tasks.md` after smoke.
