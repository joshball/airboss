# Session todos -- engine-goal-cutover

WP: `docs/work-packages/engine-goal-cutover/` (locked decisions: redirect plan UI, goal-wins, structured logger, 14d trigger, new file).

## Phases

- [ ] Phase 0: schema -- add `goal.focus_domains`, `goal.skip_domains`, `goal.skip_nodes` jsonb columns + telemetry constants in `@ab/constants`.
- [ ] Phase 1: backfill script `scripts/db/backfill-goal-targeting.ts` + dispatcher entry.
- [ ] Phase 2: BC -- `libs/bc/study/src/engine-targeting.ts` (`EngineTargeting`, `getEngineTargeting`, `getEngineTargetingSnapshot`) + goal targeting read/write helpers in `goals.ts` + tests.
- [ ] Phase 3: cut over `previewSession` to read targeting from `getEngineTargeting`. Drop `plan.certGoals` etc references in sessions.ts.
- [ ] Phase 4: integration tests for cutover paths.
- [ ] Phase 5: plan-creation UX cutover -- redirect from `/plans/new` and `/plans/[id]` to goal composer; reject non-empty `certGoals` in `createPlan`/`updatePlan`.
- [ ] Phase 7: telemetry log line + drift-check script `scripts/db/check-plan-goal-drift.ts` + `check-engine-targeting-source.ts`.
- [ ] Phase 8: backfill sanity script `scripts/db/check-goal-targeting-backfill.ts`.
- [ ] Phase 9: drop migration file (sits in tree, applied later).
- [ ] Phase 11: docs -- ADR 016 phase 6 status, NOW.md follow-on closed, cross-link from cert-syllabus WP.
- [ ] Verification: `bun run check`, grep gates, tests pass.
- [ ] Commit, push, PR.
