---
date: 2026-04-22
machine: worktree-agent-ab511ddf
branch: worktree-agent-ab511ddf
revision: 1
prompt: >
  Build Flight Dynamics Sim Phase 0 -- hand-rolled C172 FDM, three SVG
  instruments, one scenario (departure stall) on a new apps/sim surface.
  Throwaway prototype validating the worker -> snapshot -> Svelte
  instrument pipeline before the Phase 2 JSBSim port.
context: >
  Phase 0 scope from docs/work/plans/20260422-flight-dynamics-sim-plan.md.
  Builds a new SvelteKit app (apps/sim) + BC (libs/bc/sim) mirroring study.
---

# Sim Phase 0 -- Session Todo

## Completed

- [x] `libs/constants/src/sim.ts` -- scenario ids, aircraft ids, outcomes, timing, unit conversions, worker message kinds. Re-exported via `libs/constants/src/index.ts`.
- [x] `libs/constants/src/ports.ts` -- added `PORTS.SIM = 9610`.
- [x] `libs/constants/src/hosts.ts` -- added `HOSTS.SIM = 'sim.airboss.test'`.
- [x] `libs/constants/src/routes.ts` -- added `ROUTES.SIM_HOME` and typed `ROUTES.SIM_SCENARIO(id)`.
- [x] `libs/bc/sim/` -- new BC: types, C172 config, RK4 FDM, scenario registry + runner, one scenario (departure-stall). Mirrors `libs/bc/study/` layout.
- [x] FDM physics: CL/CD curves with smooth stall break, pitching moment model (stability + damping + pilot), exponential atmosphere, RK4 integrator, ground clamping. Deterministic -- no Math.random, no Date/performance.
- [x] Unit tests (Vitest) -- 16 tests covering lift curve linearity + stall, drag induced growth, 1G trim, stall onset, idle descent, scenario outcomes, determinism. All passing.
- [x] `apps/sim/` -- new SvelteKit app mirroring `apps/study/` setup: package.json, svelte.config.js, vite.config.ts, tsconfig.json, app.html/app.d.ts, layout + index + `/[scenarioId]` route.
- [x] FDM Web Worker (`apps/sim/src/lib/fdm-worker.ts`) -- 120 Hz physics loop, 60 Hz snapshot stream, typed message protocol, INIT / START / PAUSE / RESUME / RESET / INPUT handling.
- [x] Three SVG instruments (`apps/sim/src/lib/instruments/`): ASI (C172 arc markings, needle, Vspeed redline), Altimeter (3-pointer + digital), Attitude Indicator (pitch ladder, roll rotation -- zero in Phase 0). Pure SVG, no canvas, no external deps.
- [x] Cockpit page with keyboard controls: W/Up pitch-up, S/Down pitch-down, Shift throttle-up, Ctrl throttle-down, Space pause/resume, R reset. Elevator spring-returns to neutral. On-screen throttle + elevator bars show current input state.
- [x] Root `package.json` -- added `dev:study` and `dev:sim` scripts.
- [x] `scripts/dev.ts` -- sim app wired into DEV_URLS map.
- [x] `scripts/check.ts` -- svelte-check now runs on both apps/study and apps/sim.
- [x] `vitest.config.ts` -- added `@ab/bc-sim` alias.

## Quality gates

- [x] `bun run check` -- 0 errors, 0 new warnings. (Pre-existing warnings in `libs/activities/crosswind-component/CrosswindComponent.svelte` and knowledge-graph orphan references, unrelated to this work.)
- [x] `bun run test libs/bc/sim` -- 16/16 tests pass.
- [x] Biome format clean across all touched files.

## Known limitations (Phase 0 scope)

- Longitudinal 2-D only; no roll/yaw/sideslip (attitude indicator roll is wired up but always reads 0).
- IAS == TAS (no density correction beyond the density-lapse thrust).
- No sound, no outside-view graphics, no debrief, no replay, no spaced-rep integration.
- Only one scenario (`departure-stall`); only one aircraft (C172).
- Client-only -- no DB schema, no persistence of run results.
- Hand-rolled physics -- to be replaced by JSBSim WASM port in Phase 2.
