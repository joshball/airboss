---
title: 'Todo: wp-hangar-sources-v1 build'
date: 2026-04-24
session: 05
branch: worktree-agent-a153d64d34eb7b207
---

# Todo -- wp-hangar-sources-v1

End-to-end implementation of the hangar sources operations surface. See [spec](../../work-packages/hangar-sources-v1/spec.md) and [tasks](../../work-packages/hangar-sources-v1/tasks.md).

## Phases

- [x] Orientation -- read required docs, inspect current state
- [x] Phase 1 -- port `downloadFile` + UI components (DataTable, FormStack, ConfirmDialog, ValidationReport) with tests
- [x] Phase 2 -- job handlers (fetch, upload, extract, build, diff, validate, size-report) + constants
- [x] Phase 3 -- `/sources` flow diagram (new operational index)
- [x] Phase 4 -- source detail + files + diff + upload routes
- [x] Phase 5 -- `/jobs` (already present from WP2; new kinds surface automatically via `JOB_KIND_VALUES`)
- [x] Phase 6 -- nav + redirect (`/` -> `/sources`, nav as Sources | Glossary | Jobs)
- [x] Phase 7 -- polish (error boundary + empty states)
- [x] Phase 8 -- gates + PR

## Deferred (post-MVP, captured intentionally)

- **Flow-diagram arrow animation.** Spec asks for brief CSS-keyframe animation on arrows connected to running jobs. The landed implementation uses a `busy` border highlight on the tile instead (and a background tint on `tr.busy` in the source table). Upgrade: SVG `<path>` arrows between tile centres, animate `stroke-dashoffset` via `--motion-fast` on `.tile.busy`. Follow-up WP or iteration PR.
- **Per-extension PDF + CSV + Markdown rendering.** Landed as `<pre>` + inline text; the spec's `PdfPreview` (object embed) + `CsvPreview` (tabular DataTable) + `MarkdownPreview` (MarkdownBody render) are future-iteration refinements.
- **Concurrency UI indicator on 409 "source is busy".** Worker already serialises by `targetId`; the UI currently lets the user submit + sees the queued job. A "this source has a running operation, wait..." pre-check is a follow-up.

All three are strictly additive on top of the landed contract. Capture as a follow-up work package when any of them becomes a trigger.

## Out-of-scope debts captured

(none)
