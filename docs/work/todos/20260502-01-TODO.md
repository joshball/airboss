# 2026-05-02 -- break hangar 3-way package cycle

Closes critical #3 from `docs/work/reviews/2026-05-02-hangar-cluster-INDEX.md` and the convergent architecture-major "hangar-jobs not actually generic".

## Done

- Moved `hangar.job` + `hangar.job_log` Drizzle definitions out of `libs/bc/hangar/src/schema.ts` into a new `libs/hangar-jobs/src/schema.ts`. Same Postgres `hangar` namespace; only the lib ownership changed.
- Re-pointed `libs/hangar-jobs/src/{enqueue,worker,types}.ts` at the local schema (no more `@ab/bc-hangar/schema` import).
- Updated `libs/hangar-jobs/src/index.ts` to export the table + row types.
- Updated `libs/bc/hangar/src/{jobs-queries,dashboard-queries}.ts` and the BC's three job tests to import `hangarJob`/row types from `@ab/hangar-jobs`.
- Removed the table re-exports from `libs/bc/hangar/src/index.ts`.
- Moved `hangarJobHandlers` from `libs/bc/hangar/src/jobs.ts` to `apps/hangar/src/lib/server/jobs.ts`. Deleted the BC file. Re-pointed `hooks.server.ts` at the new location.
- Dropped `@ab/bc-hangar` from `libs/hangar-jobs/package.json`. Dropped `@ab/hangar-sync` from `libs/bc/hangar/package.json`. Added the `exports` block to `hangar-jobs/package.json` for parity with the other libs.
- Added `libs/hangar-jobs/src/schema.ts` to `drizzle.config.ts` schema list.
- Added `@ab/hangar-jobs/schema` deep-import alias to `vitest.config.ts`.

## Verification

- `bun run check`: hangar svelte-check 0 errors / 0 warnings; biome clean. (Pre-existing `fast-xml-parser` and `three` errors on main, unrelated.)
- `bunx vitest run --project unit-node libs/hangar-jobs libs/bc/hangar libs/hangar-sync`: 193 tests, 20 files, all pass.
- `bunx madge --circular --extensions ts libs/`: zero hangar-related cycles. The 3 cycles found are pre-existing in `bc/sim` and `sources`.
- Grep: `hangar-jobs` no longer imports from `@ab/bc-hangar`; `bc-hangar` no longer imports from `@ab/hangar-sync`. Dependency graph: `hangar-jobs` (leaf) <- `bc-hangar` <- `hangar-sync` <- app, plus app -> all three. DAG.
