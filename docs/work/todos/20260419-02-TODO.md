---
title: 'Decision Reps build session'
date: 2026-04-19
branch: worktree-agent-a314f090
---

# Decision Reps -- build session todos

Executed `docs/work-packages/decision-reps/tasks.md` in order.

## Tasks

- [x] Read work package docs (PRD, spec, design, tasks, test-plan)
- [x] Read reference implementation (spaced-memory-items code)
- [x] Read existing schema/constants/BC/index files
- [x] 1. Constants: added `DIFFICULTIES`, `PHASES_OF_FLIGHT`, id prefixes; exported
- [x] 2. Schema: added `scenario`, `rep_attempt` tables; pushed via drizzle-kit; verified in DB; committed
- [x] 3. BC: `scenarios.ts` with `createScenario`, `getScenarios`, `getNextScenarios`, `submitAttempt`, `getRepAccuracy`, `getRepStats`, `getRepDashboard`; exported
- [x] 4. Unit tests (22 DB-backed tests covering validation, priority, correctness, accuracy)
- [x] 5. Scenario creation route `/reps/new` (dynamic option builder, radio-based single correct)
- [x] 6. Scenario browse route `/reps/browse` (filters, pagination, empty states)
- [x] 7. Reps dashboard `/reps` (available, unattempted, today, 30d accuracy, domain breakdown)
- [x] 8. Rep session flow `/reps/session` (read -> confidence -> choose -> reveal -> summary)
- [x] 9. Navigation update (added Reps nav item)
- [x] Final `bun run check` (0 errors, 0 warnings)
- [x] Final `bun test` / vitest (42 tests pass)

## Deviations from spec

- Added `SCENARIO_STATUSES`, `DIFFICULTY_LABELS`, `PHASE_OF_FLIGHT_LABELS`, `SCENARIO_OPTIONS_MIN/MAX`, `REP_BATCH_SIZE`, `REP_DASHBOARD_WINDOW_DAYS` alongside the spec-listed constants. All are aliases for values the spec called out in prose or inherited from the cards BC; they give the BC + UI a single-source-of-truth the same way the cards side works.
- `getRepDashboard` returns a superset of what the spec listed (`scenariosByDomain`, `unattemptedCount`). Used by the dashboard UI; does not break the calibration-consumer contract since those live on `getRepAccuracy` / `getRepStats`.
- Added a vitest setupFile (`vitest.setup.ts`) to load `.env` into `process.env` before tests -- vite/vitest doesn't auto-load non-VITE_ prefixed vars, and the shared db module requires `DATABASE_URL` at import time.

## Bugs flagged (sibling code)

- None found in spaced-memory-items or elsewhere that I touched. The subquery-correlation bug I fixed (`"id"` binding to `rep_attempt.id` inside the correlated subquery) is on code I wrote in this feature, not a sibling.
