---
title: 'Decision Reps build session'
date: 2026-04-19
branch: worktree-agent-a314f090
---

# Decision Reps -- build session todos

Executing `docs/work-packages/decision-reps/tasks.md` in order.

## Tasks

- [x] Read work package docs (PRD, spec, design, tasks, test-plan)
- [x] Read reference implementation (spaced-memory-items code)
- [x] Read existing schema/constants/BC/index files
- [ ] 1. Constants: add `DIFFICULTIES`, `PHASES_OF_FLIGHT`, id prefixes; export
- [ ] 2. Schema: add `scenario`, `rep_attempt` tables; push; verify; commit
- [ ] 3. BC: `scenarios.ts` with `createScenario`, `getScenarios`, `getNextScenarios`, `submitAttempt`, `getRepAccuracy`, `getRepStats`; exports
- [ ] 4. Unit tests for BC behaviors
- [ ] 5. Scenario creation route `/reps/new`
- [ ] 6. Scenario browse route `/reps/browse`
- [ ] 7. Reps dashboard `/reps`
- [ ] 8. Rep session flow `/reps/session`
- [ ] 9. Navigation update (add Reps nav item)
- [ ] Final `bun run check`, `bun test`

## Notes

- Confidence input: inline on rep session page; do NOT pre-factor into `libs/ui` (Calibration work).
- Schema push via `bunx drizzle-kit push` (no generate/migrate).
- Do NOT run manual test plan; do NOT push branch or open PR; do NOT merge.
