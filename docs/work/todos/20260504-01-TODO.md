# 2026-05-04: WP-FLIGHTBAG-BOOK-EXPERIENCE build

Implement six phases of the book-experience WP. Spec: `docs/work-packages/wp-flightbag-book-experience/spec.md`.
Auth resolution: `docs/decisions/024-cross-app-auth-identity-roles-entitlements.md`.

## Phase 1 (this PR): reading-order BC primitive

- [x] `getReadingOrder(referenceId)` BC function in `libs/bc/study/src/references.ts`
- [x] `computeReadingOrder` pure helper (depth-first walk + chapter-context resolution + word count)
- [x] `getNextInReadingOrder` / `getPreviousInReadingOrder`
- [x] Unit tests over synthetic trees (handbook / AIM nesting / whole-doc / CFR / edge cases)
- [x] Export from BC barrel
- [x] Existing flightbag `computeSiblingNav` already covers route-side nav -- BC primitive is the canonical reading order for upcoming TOC drawer + reading-time UI

## Phase 2: Breadcrumbs component (next PR)

Extract inline `<nav class="crumbs">` from every reader page to `<Breadcrumbs>` in `libs/library/`.

## Phase 3: TOC drawer

`<TOCDrawer>` consuming `getReadingOrder`. Persistent left-rail, collapsible.

## Phase 4: chapter-preamble verification

Verify Bucket A (PR #584) preamble surfacing across every handbook + AC.

## Phase 5: reading-time estimates

Display `wordCount / WORDS_PER_MINUTE_READING_RATE` per section + chapter aggregate.

## Phase 6: read state + heartbeat (gated on entitlement primitive)

- Auth gate: `requireAuth` (entitlement primitive lands separately per ADR 024).
- Heartbeat endpoint mirroring `apps/study/.../heartbeat/+server.ts`.
- TOC drawer checkmarks for sections with `read_count > 0`.
