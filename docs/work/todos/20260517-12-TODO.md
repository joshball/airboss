# 2026-05-17 -- Close two deferred review findings

Branch: `worktree-agent-a41afc67fd2f6ba95`

Two findings a project-wide review left deferred as "out of scope for this slice."

## Finding 1 -- ID-prefix magic strings in `libs/utils/src/ids.ts`

- [x] Define `*_ID_PREFIX` constants for every generator still using an inline literal
  - study BC -> `libs/constants/src/study.ts`
  - audit BC -> `libs/constants/src/audit.ts`
  - hangar jobs -> `libs/constants/src/jobs.ts`
  - hangar review queue -> `libs/constants/src/review.ts`
  - sim BC -> `libs/constants/src/sim.ts`
- [x] Re-export each alphabetically from `libs/constants/src/index.ts`
- [x] Route every generator through its constant
- [x] Verify zero inline `createId('...')` literals remain (`generateAuthId` uses `ulid()`, left as-is)

## Finding 2 -- `cardEnum` widening in `libs/bc/study/src/validation.ts`

- [x] Make the 9 `*_VALUES` arrays consumed by `validation.ts` literal tuples
      (`as [T, ...T[]]` on the `Object.values(...)` result -- keeps the literal
      union element type, satisfies `z.enum`, stays assignable to `T[]`)
- [x] Remove the `as [string, ...string[]]` casts in `cardEnum` / `scenarioEnum` /
      `assessmentMethodsSchema` / teaching-exercise schemas
- [x] Remove the now-unnecessary downstream enum-widening casts at the card /
      scenario creation call sites (flightbag cards API, memory/[id], memory/new,
      reps/new)
- [x] Verify with the type checker

## Out of scope (related, not in this slice)

`plans.validation.ts`, `plan-items.validation.ts`, and `libs/bc/hangar/` form
schemas use the same `z.enum(X as [string, ...string[]])` widening pattern over
separately-declared `Object.values(...)` arrays. Not touched -- the brief scoped
Finding 2 to `cardEnum`. Flagged in the PR description as a follow-up candidate.
