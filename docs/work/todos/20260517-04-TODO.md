# Section 2 review + fixes -- Weather + Content Authoring

Branch: `review/section-2-weather-content-authoring`
Review: `docs/work/reviews/2026-05-17-section-2-weather-content-authoring.md`

## Fixes -- all complete

- [x] M1 -- METAR `M1/4SM` visibility fabricated (drop the `/ 2`)
- [x] M2 -- convective `why` dead for CB clouds (capture CB into CloudLayer)
- [x] M3 -- convective `why` skips moderate TS (gate on token, not family)
- [x] m1 -- raw `err.message` leaked on 500 in glossary/sources syncAll
- [x] m2 -- unhandled `applyOverride` throw in ingest-review action
- [x] m3 -- orphan recovery re-queues live jobs (heartbeat gate)
- [x] m4 -- `err.message` as formError on NotFoundError (glossary delete x2)
- [x] m5 -- token hole `--type-ui-section-size` (4 files)
- [x] n1 -- `honest`/`dishonest` in agent-voice comments (~40 sites)
- [x] n2 -- dead clause in METAR wind sensor-out check
- [x] n3 -- magic `12` default TAF valid-hours -> constant
- [x] n4 -- magic `1.94384` -> `MPS_TO_KNOTS`; add `HPA_TO_INHG`
- [x] n5 -- `{#each}` keyed by free text -> keyed by index

## Verify -- complete

- [x] `bun run check branch` -- 0 errors / 0 warnings
- [x] wx-charts/wx-engine/wx-explain/content-census -- 571 tests pass
- [x] ingest-review/bc-hangar/hangar-sync -- 456 tests pass
- [x] hangar-jobs -- 17 tests pass (incl. new live-worker recovery test)
- [x] re-grep each symptom -- clean
</content>
