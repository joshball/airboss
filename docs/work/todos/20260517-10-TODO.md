# 2026-05-17 -- Study surface 10x review + fix

Review scope: `apps/study/`, `libs/bc/study/`, `libs/help/`.

## Findings to fix

- [x] M1 -- `study/+page.server.ts` `setPref` 500 leak: now logs raw error, returns fixed string.
- [x] M2 -- `resolveGoalPrimaryCredential` raw SQL -> `getGoalPrimaryCredentialId` BC fn (Drizzle).
- [x] M3 -- `build-course-tree.server.ts` `resolveKnowledgeSlugsToIds` -> reuses `getNodesByIds`.
- [x] M4 -- `build-course-tree.server.ts` `resolveAcsCodesToLeafIds` -> `getSyllabusNodesByCodes` BC fn.
- [x] M5 -- `build-today-briefing.server.ts` node-title raw SQL -> `getKnowledgeNodeTitles` BC fn.
- [x] m1 -- `snooze.ts` raw `"card_edited_at"` string -> `isNotNull(cardSnooze.cardEditedAt)`.
- [x] m2 -- `study/+page.server.ts` inline `'/study'` -> `ROUTES.STUDY`.
- [x] m3 -- `memory/drafts` `promote` raw err.message leak -> logged + fixed string.
- [x] m4 -- `memory/drafts` raw `confirm()` -> `ConfirmAction` primitive.
- [x] m5 -- `memory/drafts` `promote` redirect moved out of the `try` block.

## Tests added

- [x] `getGoalPrimaryCredentialId` -- 3 cases
- [x] `getSyllabusNodesByCodes` -- 3 cases
- [x] `getKnowledgeNodeTitles` -- 3 cases

## Verify

- [x] `bun run check branch` 0/0
- [x] relevant tests pass (124 in goals/syllabi/knowledge; barrel-split 67)
- [x] re-grep symptoms gone
