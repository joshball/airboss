# 2026-05-17 -- Study surface 10x review + fix

Review scope: `apps/study/`, `libs/bc/study/`, `libs/help/`.

## Findings to fix

- [ ] M1 -- `study/+page.server.ts:300` leaks raw `err.message` on a 500 (`setPref` action). Map to a fixed user string + log raw via `createLogger`.
- [ ] M2 -- `resolveGoalPrimaryCredential` in `study/+page.server.ts` does raw `db.execute(sql\`...\`)` in a route file. Move to a `getGoalPrimaryCredentialId` BC fn in `goals.ts`, Drizzle query.
- [ ] M3 -- `build-course-tree.server.ts` `resolveKnowledgeSlugsToIds` raw SQL -> reuse `getNodesByIds` from `@ab/bc-study`.
- [ ] M4 -- `build-course-tree.server.ts` `resolveAcsCodesToLeafIds` raw SQL -> new `getSyllabusNodesByCodes` BC fn in `syllabi.ts`.
- [ ] M5 -- `build-today-briefing.server.ts` raw SQL `SELECT title FROM knowledge_node` -> new `getKnowledgeNodeTitles` BC fn in `knowledge.ts`.
- [ ] m1 -- `snooze.ts:345` raw SQL string `"card_edited_at" IS NOT NULL` -> Drizzle `isNotNull(cardSnooze.cardEditedAt)`.
- [ ] m2 -- `study/+page.server.ts:110` inline route string `'/study'` -> `ROUTES.STUDY_HOME`.
- [ ] m3 -- `memory/drafts/+page.server.ts:95` `err instanceof Error => fail(400, err.message)` leaks raw error text for non-typed errors. Map to fixed string + log.
- [ ] m4 -- `memory/drafts/+page.svelte:134` raw `window.confirm()` on the Discard action -> `ConfirmAction` primitive (other danger actions in study use it).
- [ ] m5 -- `memory/drafts/+page.server.ts` `promote` action calls `redirect()` inside `try`; relies on the catch's `err instanceof Error` being false to fall through. Move redirect out of try via `isRedirect` re-throw or restructure.

## Verify

- [ ] `bun run check branch` 0/0
- [ ] relevant tests pass
- [ ] re-grep symptoms gone
