# 2026-04-27 -- scripts restructure to `bun run sources`

## Goal

Replace the three top-level scripts (`download-sources`, `ingest`, `handbook-ingest`) with a single umbrella dispatcher `scripts/sources.ts` that follows the established `db.ts` / `references.ts` pattern. Carve the 1,079-line `download-sources.ts` into a folder of focused modules. Extract genuine cross-script duplication into a new `scripts/lib/` directory. Add yellow/green/red coloring to the download output. Update every doc/work-package/ADR reference. Pure restructure + dedup + colors -- ZERO semantic changes to URL plans, manifest format, freshness rules, or Python ingest behavior.

## Status

Done.

- [x] `scripts/lib/` -- shared utilities (`spawn`, `prompt`, `bytes`, `error`, `cache`, `sleep`, `http-date`, `colors`)
- [x] `scripts/sources/download/` -- 8 carved modules (`args`, `plans`, `manifest`, `http`, `freshness`, `symlink`, `ecfr`, `summary`, `execute`, `run`, `index`, `constants`)
- [x] `scripts/sources/download.ts` -- entry shim
- [x] `scripts/sources/register.ts` + `register/{cfr,handbooks,aim}.ts` delegators
- [x] `scripts/sources/extract.ts` + `extract/handbooks.ts` (Python dispatcher)
- [x] `scripts/sources.ts` -- top-level dispatcher with `COMMAND_HELP` + `COMMAND_GROUPS`
- [x] `scripts/sources/{download,register}.test.ts` moved + import paths fixed
- [x] Old top-level files deleted (`download-sources.ts`, `download-sources.test.ts`, `handbook-ingest.ts`, `ingest.ts`, `ingest.test.ts`, `README.download-sources.md`)
- [x] Dedup retrofits: `db.ts`, `references.ts`, `smoke.ts`, `test.ts`, `db/seed-all.ts` import from `scripts/lib/`
- [x] `package.json` -- single `sources` script replaces three old entries
- [x] Colors: yellow skipped / green ok / red errors, NO_COLOR + non-TTY aware
- [x] `scripts/README.sources.md` -- new operator runbook
- [x] Doc replacements across `docs/`, `tools/`, `tests/`, plus `apps/study/src/routes/(app)/handbooks/+page.svelte`
- [x] `bunx vitest run scripts/sources/` -- 41 tests pass
- [x] `bun scripts/sources.ts download --dry-run --corpus=ac` -- output matches old shape (now colored)
- [x] `bunx biome check` -- clean on changed files
- [x] grep for stale refs returns zero hits across `docs/`, `scripts/`, `tools/`, `tests/`, `apps/`, `libs/`
