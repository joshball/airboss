# 2026-05-18 -- XC Viewer v1 work package (Phases A-F)

- Machine: worktree agent-a0a9082cdcc684014
- Branch: feature branch per phase
- Trigger: Implement the `xc-viewer-v1` work package end to end.

## Phases

- [ ] Phase A -- scaffold + types + Memphis sectional ingest
- [ ] Phase B -- sectional renderer
- [ ] Phase C -- route + waypoint renderer + leg labels
- [ ] Phase D -- weather overlay
- [ ] Phase E -- aircraft performance
- [ ] Phase F -- SvelteKit page + directive + course-step mount + check wire-in

## Notes

- `frontal-xc-march` wx scenario covers KSTL->KORD (Illinois); the v1 route
  KMEM/KMKL/KOLV is in the Memphis sectional (TN/MS). Per spec, the weather
  layer projects nearest-station METAR/TAF to each waypoint and AIRMET
  membership via point-in-polygon -- the geographic offset is an accepted v1
  simplification (killer-feature thesis is "claims are derivable").
- FAA dCS source archive not present in the dev cache. The ingester reads dCS
  archives when present and surfaces a clear "archive not found" error; the
  Memphis vector geometry for v1 is hand-authored from public FAA chart data.
