# 2026-04-19 -- Learning Dashboard v1 build

Branch: `worktree-agent-a34bde27`

## Done

- [x] Read work package docs (PRD, spec, design, tasks, test-plan) plus sibling specs (SMI, reps, calibration, knowledge-graph, study-plan)
- [x] Add `ROUTES.DASHBOARD` to `libs/constants/src/routes.ts`
- [x] Add `WEAK_AREA_*`, `ACTIVITY_WINDOW_DAYS`, `OVERDUE_GRACE_MS` constants in `libs/constants/src/study.ts`
- [x] Author `libs/bc/study/src/dashboard.ts` with `getRepBacklog`, `getRecentActivity`, `getWeakAreas`, `getDashboardPayload` (injectable fetchers for testability)
- [x] Unit tests for all four functions -- 12/12 passing against local Postgres
- [x] `/dashboard` route (`+page.server.ts` + `+page.svelte`) rendering all 9 panels
- [x] Panel components: CTA, DueReviews, ScheduledReps, Calibration (gated), WeakAreas, Activity (CSS sparkline), CertProgress (gated), Map (gated), StudyPlan (gated)
- [x] `PanelShell.svelte` shared error boundary / visual rhythm
- [x] Root `(app)/+page.server.ts` redirect now targets `ROUTES.DASHBOARD` (was `ROUTES.MEMORY`)
- [x] Nav layout gains `Dashboard` entry (first in order) with `aria-current`
- [x] `/memory/review` "Back to dashboard" link now points at `/dashboard` to match its label
- [x] `bun run check` clean (0/0) after each task; `bun test` green (55/55)
- [x] NOW.md roadmap row for Learning Dashboard updated

## In flight

- [ ] Push branch and open PR to `main` (don't merge)

## Handoff

- Manual test plan: `docs/work-packages/learning-dashboard/test-plan.md` -- LD-1 through LD-20 (LD-21 and LD-22 are gated on sibling features)
- Gated panels (calibration, cert progress, map, study plan) render placeholder cards visibly distinct from live panels -- swap-in work lives in the sibling work packages' PRs

## Coordination notes

- Calibration panel is a placeholder in this PR because `libs/bc/study/src/calibration.ts` is not yet on main (authored in agent-ae7544b0 worktree). When that PR lands, the panel is a drop-in replacement reading `getCalibration(userId)`.
- Cert progress and map panels wait on the knowledge-graph work package's `getCertProgress` and `getDomainCertMatrix`.
- Study plan panel waits on the study-plan-and-session-engine work package's `getActivePlan`.
- CTA currently forks between memory review and rep session. Once the session engine lands, the CTA target becomes the session engine's start URL.
