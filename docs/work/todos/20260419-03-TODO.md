# Session Todos -- 2026-04-19 (Calibration Tracker)

Triggering prompt: build calibration-tracker work package (Step 3 of roadmap).

## Tasks

1. [x] Read work package docs (PRD, spec, tasks, test-plan)
2. [x] Read existing schema, BC, constants, review + session pages
3. [x] Create `libs/ui/src/components/ConfidenceSlider.svelte` + export from index
4. [x] Refactor `(app)/memory/review/+page.svelte` to use shared slider
5. [x] Refactor `(app)/reps/session/+page.svelte` to use shared slider
6. [x] Run `bun run check` green; commit
7. [x] Create `libs/bc/study/src/calibration.ts` (getCalibration, getCalibrationTrend, getCalibrationPointCount)
8. [x] Export from `libs/bc/study/src/index.ts`
9. [x] `libs/bc/study/src/calibration.test.ts` covering bucket rules, score, empty, combined sources, trend, domain filter
10. [x] `bun test` green (54/54); commit
11. [x] Create `apps/study/src/routes/(app)/calibration/+page.server.ts`
12. [x] Create `apps/study/src/routes/(app)/calibration/+page.svelte`
13. [x] Add nav item to `(app)/+layout.svelte`
14. [x] `bun run check` green
15. [ ] Push branch + open PR

## Notes

- ROUTES.CALIBRATION already exists in `libs/constants/src/routes.ts`.
- `CONFIDENCE_LEVELS`, `CONFIDENCE_LEVEL_VALUES`, `CONFIDENCE_SAMPLE_RATE` all exist.
- Schema already has `review.confidence` + `rep_attempt.confidence` (smallint 1..5, nullable).
- No schema changes required.
