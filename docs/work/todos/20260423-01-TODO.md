# Session todo: 2026-04-23 wp-hangar-scaffold

Branch: `worktree-agent-a6413a36` (isolated worktree)
PR target: `main`
Plan: [docs/work/todos/20260422-hangar-data-management-plan.md](./20260422-hangar-data-management-plan.md) (wp-hangar-scaffold section)

## Done

- [x] Extracted `libs/audit/` from `@ab/db`; exports `auditWrite`, `auditRecent`, `auditLog`, `AUDIT_OPS`, types. Removed audit re-exports from `@ab/db`. Added `@ab/audit` path alias to root tsconfig.
- [x] Added constants: `HOSTS.HANGAR`, `PORTS.HANGAR` (9620), `ROUTES.HANGAR_HOME`, `AUDIT_TARGETS.HANGAR_PING`.
- [x] Scaffolded `apps/hangar/` -- workspace, svelte + vite + tsconfig, CSP, app shell, hooks, server auth wiring, root layout with role gate.
- [x] `/login` and `/logout` routes ported from study pattern.
- [x] Home page proves the full stack: `?/ping` form action writes one `hangar.ping` audit row; `load` reads last 10 rows for display.
- [x] `scripts/dev.ts` multi-spawns hangar. `scripts/check.ts` runs svelte-check against hangar.
- [x] `bun run check` clean for my changes (my svelte-check surface is 0/0).
- [x] Work package docs: `docs/work-packages/hangar-scaffold/{spec,tasks,test-plan}.md`.
- [x] Commits landed in logical chunks: audit extraction / constants / scaffold / home page.
- [x] PR opened (URL in agent report).

## Developer follow-ups (outside this session)

- Add `127.0.0.1 hangar.airboss.test` to `/etc/hosts` (or run `bun run setup` which prints the exact line).
- Manual test-plan walkthrough before merge.

## Carry-forwards for later WPs

- **wp-hangar-registry** is next per the plan. Nothing from this WP spills into it; the scaffold is self-contained.
- **Shared hangar nav** -- the top-bar nav in hangar today is just identity + sign-out. Cross-section nav lands in `wp-hangar-sources-v1` once there's more than one surface to link to.
- **Playwright e2e for the scaffold** -- deferred to `wp-hangar-sources-v1` test plan (first WP that has enough surface to justify a browser test budget).
- **Pre-existing `bun run check` failure** -- `scripts/references.ts validate` fails on main with a module-resolution error from `apps/study/src/lib/help/content/calibration.ts`. Unrelated to this WP. Surface it to the user; don't try to fix here.

## Observations

- Airboss's `libs/auth/` already has `requireAuth`, `requireRole`, `requireVerifiedEmail`. The plan anticipated a port from FIRC; nothing needed porting beyond the extraction of audit.
- Airboss's role enum already carries the 4 values the plan called out.
- `scripts/setup.ts` iterates `HOSTS.values` so `HOSTS.HANGAR` drops in cleanly; no edits to setup were needed.
