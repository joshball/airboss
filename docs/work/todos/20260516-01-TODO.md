# 2026-05-16 -- Session todo

DateTime: 2026-05-16
Branch: main (orchestrator)
Context: Phase 3 directive cleanup + callout pedagogy design + re-dispatch of
tracks lost to socket errors on 2026-05-15.

## Decided this session (callout design)

- Callouts split into two kinds. Kind-1 (`note`, `example`) = reading aids, no
  cards. Kind-2 (`tip`, `warn`) = retention-bearing, must declare cards.
- Four variants kept (`tip`, `warn`, `note`, `example`); `danger` + `howto`
  retired as dead `CalloutVariant` members.
- `:::tip` syntax stays. Callouts become a registered directive family; the
  variant is the directive name.
- Inline-closed directive form added: `:::cards none:::` on one line. General
  rule for any body-bearing directive. `:::cards none:::` + stray `:::` -> throw.
- Kind-2 callouts must contain exactly one `:::cards` (items or `none`).
  Build-time enforcement, switched ON only after the 35-callout backfill.
- Shipped as a PLAN (`docs/work/plans/`), not a `/ball-wp-spec` work package.
- True corpus counts: 25 `:::tip` + 10 `:::warn` = 35 Kind-2 to triage.
  18 `:::note` + 12 `:::example` Kind-1, untouched.

## Done (all five tracks merged)

- [x] Track A -- Phase 3 directive cleanup (PR #1016, callout family, 4 variants,
      danger/howto retired, inline-closed `:::name ...:::` form, enforcement OFF).
- [x] Track A-plan -- retention-bearing-callouts plan + 35-callout triage list
      (PR #1010).
- [x] Track B -- 4 walkthrough scaffolds (PR #1012: wx-engine,
      course-tree-arbitrary-depth, card-question-tier, weather-comprehensive).
- [x] Track C -- hangar hydration-leak fix (PR #1014, rebased + merged;
      self-triggering `$effect` wrapped in `untrack()`).
- [x] Track D -- 2 WP sign-off review summaries (PR #1011).
- [x] Cleanup -- 6 worktrees removed, 9 local + 3 remote branches deleted.

## Follow-up done (PR #1017)

- [x] xc-viewer-v1 port collision fixed -- `PORTS.SPATIAL = 9650` added,
      spec/tasks/design repointed. WP build unblocked.
- [x] retention-callout plan rescoped -- all 35 triaged callouts were
      help-content tool guidance, not study content. Triage list deleted;
      plan now scopes the card requirement to `course/knowledge/**` only,
      help content exempt. No backfill needed (study corpus has no callouts).

## Awaiting Joshua

- WP sign-off on course-reader-and-editor + xc-viewer-v1 (human-only).
  - course-reader-and-editor: already fully built and merged (PRs #767-776);
    sign-off is retroactive. Reviewer suggests running `/ball-review-full`
    and the manual walkthrough first.
  - xc-viewer-v1: port blocker now cleared; build-ready once you sign off.

## Known unrelated debt (not mine, surfaced during check)

- `docs/work/SHIPPED.md` is stale (this session's PRs not regenerated) --
  `bun run track generate` refreshes it.
- 3 untracked `docs/work/wind-down/2026-05-15/*.md` files are unformatted.
