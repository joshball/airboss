# 2026-05-17 -- hangar-content-census Phase 1

Branch: worktree-agent-a96cd1de1c111c8fd. Worktree session.

Triggering prompt: build Phase 1 of the `hangar-content-census` work package --
the `/content` census dashboard, the wx-catalog reference drill-down, honest
stubs for the other 13 corpora, the content-intent ADR.

## Done

- [x] `libs/content-census/` lib -- `package.json`, runtime barrel (`index.ts`),
      `/server` barrel, `types.ts`, `registry.ts` (14 corpora), `overview.ts`
      pure helpers.
- [x] `wx-catalog.server.ts` -- full reference adapter: 155-example inventory,
      real metrics, three explained gaps, value-ranked next-list.
- [x] `_stub.server.ts` -- honest placeholder adapter for the other 13 corpora.
- [x] `ROUTES.CONTENT_CENSUS` + `ROUTES.CONTENT_CENSUS_CORPUS(id)` in constants.
- [x] `/content` overview route + `/content/[corpus]` generic drill-down route.
- [x] Nav link + `/content` <-> `/roadmap` cross-links.
- [x] ADR 028 -- content-intent frontmatter, status `proposed`.
- [x] Unit tests (wx-catalog counts, explanatory-rule guard, stub honesty).
- [x] e2e smoke `tests/e2e/content-census-smoke.spec.ts`.
- [x] tsconfig / svelte.config / vitest.config / playwright.config wiring.

## Notes

- The spec names a `/platform` route (`hangar-platform-dashboard`) for the
  cross-link; that route does not exist in the repo. `/roadmap` is the shipped
  process-metadata dashboard, so the cross-link points there. Recorded in
  tasks.md.

## Remaining (Phase 2/3 -- out of this PR's scope)

- Real Layer-1 adapters for the 13 stub corpora (tasks.md Phase 2).
- Per-corpus Layer-2 intent frontmatter + gap/next (tasks.md Phase 3).
- Manual reader-with-no-context walkthrough of `/content/wx-catalog`.
