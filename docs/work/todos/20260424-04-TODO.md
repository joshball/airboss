---
title: 'wp-hangar-registry phases 6-10: UI + nav + theme'
date: 2026-04-24
session: 04
status: active
---

# Phases 6-10 (registry UI + nav + theme)

Single-PR scope: /glossary + /glossary/sources + /jobs + nav + theme gates.

## Phase 6 — /glossary

- [ ] Restructure routes under `(app)` layout group
- [ ] `(app)/+layout.svelte` with Nav + ThemeProvider + appearance toggle
- [ ] `(app)/+page.server.ts` redirect to /glossary
- [ ] `/glossary/+page.server.ts` list with filters (search / sourceType / knowledgeKind / flightRules / dirty)
- [ ] `/glossary/+page.svelte` table + URL-synced filters + "Sync all pending"
- [ ] `/glossary/new/+page.{server.ts,svelte}` create form + tag pickers + markdown preview
- [ ] `/glossary/[id]/+page.{server.ts,svelte}` edit + rev lock + 409 UI + soft-delete

## Phase 7 — /glossary/sources

- [ ] list + new + [id] mirror with fewer fields

## Phase 8 — /jobs

- [ ] `/jobs/+page.{server.ts,svelte}` list with poll
- [ ] `/jobs/[id]/+page.{server.ts,svelte}` detail with 1 Hz log poll
- [ ] `/jobs/[id]/log/+server.ts` cursor-based JSON endpoint

## Phase 9 — Nav + redirect

- [ ] `Nav.svelte` with Glossary / Sources / Jobs
- [ ] Root /+page.server.ts redirect (covered by (app)/+page)
- [ ] Move audit-ping demo to /admin/audit-ping

## Phase 10 — Theme + FOUC

- [ ] app.html pre-hydration script
- [ ] appearance toggle persists via cookie
- [ ] hooks.server reads cookie into locals.appearance
- [ ] /appearance endpoint
- [ ] theme-lint clean

## Tests

- [ ] glossary server-action unit tests (create/update/rev conflict/soft-delete)
- [ ] sources server-action unit tests
- [ ] sync-all-pending integration test
- [ ] job-log polling endpoint test
- [ ] theme-token test per new page (no hex in compiled CSS)
