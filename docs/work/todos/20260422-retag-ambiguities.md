# Retag ambiguities report (2026-04-22)

Generated by `scripts/references/migrate-firc-entries.ts` during the 175-entry
port from airboss-firc. Each entry below landed in `libs/aviation/src/references/aviation.ts`
with best-guess tags, but the script flagged one or more reasons a CFI should eyeball
the result.

Totals:

- Entries ported: **175** / 175
- Reverse `related` edges added for bidirectional symmetry: **144**
- Entries with at least one ambiguity flag: **38**
- Total flags: **46**

## Triage workflow

1. Read each flagged entry's current tags in `libs/aviation/src/references/aviation.ts`.
2. Cross-check against the entry's `detail` content.
3. Edit the file directly -- the migration script is one-shot.
4. Re-run `bun run check` after edits.

## Flags by entry

### `ac-reg` -- AC

- Only one topic inferred (regulations). Likely benefits from multi-topic review.
- Source string 'FAA.gov' did not match any sourceType parser -- fell back to 'authored'.

### `agl-ops` -- AGL

- No topic inferred from id/domain/group or content keywords -- fallback regulations.
- Only one topic inferred (regulations). Likely benefits from multi-topic review.
- Source string 'AIM Pilot/Controller Glossary' not parseable into SourceCitation -- sources[] empty.

### `als-ops` -- ALS

- Only one topic inferred (airports). Likely benefits from multi-topic review.

### `asr-atc` -- ASR

- Only one topic inferred (communications). Likely benefits from multi-topic review.

### `cdi-nav` -- CDI

- Only one topic inferred (navigation). Likely benefits from multi-topic review.

### `ceiling-wx` -- Ceiling

- Only one topic inferred (weather). Likely benefits from multi-topic review.

### `da-ops` -- DA

- Only one topic inferred (procedures). Likely benefits from multi-topic review.

### `dh-ops` -- DH

- Only one topic inferred (procedures). Likely benefits from multi-topic review.

### `dme-nav` -- DME

- Only one topic inferred (navigation). Likely benefits from multi-topic review.

### `efvs-nav` -- EFVS

- Only one topic inferred (navigation). Likely benefits from multi-topic review.

### `ffs-training` -- FFS

- Only one topic inferred (training-ops). Likely benefits from multi-topic review.

### `flight-visibility-wx` -- Flight visibility

- Only one topic inferred (weather). Likely benefits from multi-topic review.

### `fstd-training` -- FSTD

- Only one topic inferred (training-ops). Likely benefits from multi-topic review.

### `ftd-training` -- FTD

- Only one topic inferred (training-ops). Likely benefits from multi-topic review.

### `gps-nav` -- GPS

- Only one topic inferred (navigation). Likely benefits from multi-topic review.

### `ground-visibility-wx` -- Ground visibility

- Only one topic inferred (weather). Likely benefits from multi-topic review.

### `icao-org` -- ICAO

- Source string 'ICAO Annexes' did not match any sourceType parser -- fell back to 'authored'.

### `im-nav` -- IM

- Only one topic inferred (navigation). Likely benefits from multi-topic review.

### `imc-wx` -- IMC

- Only one topic inferred (weather). Likely benefits from multi-topic review.

### `mca-ops` -- MCA

- No topic inferred from id/domain/group or content keywords -- fallback regulations.
- Only one topic inferred (regulations). Likely benefits from multi-topic review.

### `mea-ops` -- MEA

- No topic inferred from id/domain/group or content keywords -- fallback regulations.
- Only one topic inferred (regulations). Likely benefits from multi-topic review.

### `mel-ops` -- MEL

- Only one topic inferred (maintenance). Likely benefits from multi-topic review.

### `moca-ops` -- MOCA

- No topic inferred from id/domain/group or content keywords -- fallback regulations.
- Only one topic inferred (regulations). Likely benefits from multi-topic review.

### `mra-ops` -- MRA

- No topic inferred from id/domain/group or content keywords -- fallback regulations.
- Only one topic inferred (regulations). Likely benefits from multi-topic review.

### `msl-ops` -- MSL

- Only one topic inferred (flight-instruments). Likely benefits from multi-topic review.
- Source string 'AIM Pilot/Controller Glossary' not parseable into SourceCitation -- sources[] empty.

### `mvfr-wx` -- MVFR

- Only one topic inferred (weather). Likely benefits from multi-topic review.

### `ndb-nav` -- NDB

- Only one topic inferred (navigation). Likely benefits from multi-topic review.

### `obs-nav` -- OBS

- Only one topic inferred (navigation). Likely benefits from multi-topic review.

### `over-the-top-wx` -- Over-the-top

- Only one topic inferred (weather). Likely benefits from multi-topic review.

### `par-atc` -- PAR

- Only one topic inferred (communications). Likely benefits from multi-topic review.

### `pirep-wx` -- PIREP

- Only one topic inferred (weather). Likely benefits from multi-topic review.

### `raim-nav` -- RAIM

- Only one topic inferred (navigation). Likely benefits from multi-topic review.

### `sigmet-wx` -- SIGMET

- Only one topic inferred (weather). Likely benefits from multi-topic review.

### `tacan-nav` -- TACAN

- Only one topic inferred (navigation). Likely benefits from multi-topic review.

### `taf-wx` -- TAF

- Only one topic inferred (weather). Likely benefits from multi-topic review.

### `vor-nav` -- VOR

- Only one topic inferred (navigation). Likely benefits from multi-topic review.

### `waas-nav` -- WAAS

- Only one topic inferred (navigation). Likely benefits from multi-topic review.

### `wings-reg` -- WINGS

- Only one topic inferred (regulations). Likely benefits from multi-topic review.

