# 2026-05-17 -- z.enum tuple-widening cleanup

Closing the convergent code-quality defect: `z.enum(X as [string, ...string[]])`
type-widening anti-pattern. PR #1060 fixed `cardEnum`; this finishes the cluster.

## Done

- [x] Tightened `*_VALUES` arrays to assert tuple shape at declaration
      (`Object.values(...) as [T, ...T[]]`), mirroring PR #1060's `DOMAIN_VALUES` /
      `CARD_TYPE_VALUES` pattern: `CERT_VALUES`, `PLAN_STATUS_VALUES`,
      `PLAN_ITEM_KIND_VALUES`, `SESSION_MODE_VALUES`, `DEPTH_PREFERENCE_VALUES`
      (study.ts); `SOURCE_TYPE_VALUES`, `AVIATION_TOPIC_VALUES`,
      `FLIGHT_RULES_VALUES`, `KNOWLEDGE_KIND_VALUES`,
      `REFERENCE_PHASE_OF_FLIGHT_VALUES`, `CERT_APPLICABILITY_VALUES`
      (reference-tags.ts); `ROLE_VALUES` (roles.ts); `INVITABLE_ROLE_VALUES`
      (invitation-schemas.ts).
- [x] Removed `as [string, ...string[]]` / `as unknown as [string, ...string[]]`
      casts at every `z.enum(...)` site in plans.validation.ts,
      plan-items.validation.ts, user-write-schemas.ts, form-schemas.ts,
      invitation-schemas.ts, api/plan-items/+server.ts.
- [x] Removed redundant downstream recovery casts in plan-items.ts and plans.ts;
      dropped the now-unused `PlanItemKind` type import in +server.ts.
- [x] `bun run check branch` -- 0 errors / 0 warnings (all 5 svelte-check apps).
- [x] Re-grep clean: zero `as [string, ...string[]]` hits in source files.
