# R7 Discovery Surface

WP: `docs/work-packages/apply-errata-and-afh-mosaic/`. Phase R7 only.

## Done

- [x] Read spec, design, tasks, test-plan, research dossier, ADR 020
- [x] Survey existing files (cli.py, plans.ts, hooks.server.ts, plugin registry, YAML configs)
- [x] Symlink venv into worktree so python tests run
- [x] Capture pre-existing `bun run check` failures (fast-xml-parser, theme-lint on R6 components, biome on R6 tests)

## Plan (this session)

- [x] Constants: discovery cache paths, GH issue label, dismissal banner, freshness window, env var name
- [x] Catalogue (TS): all 17 FAA handbooks with parent-page URL + actionable flag
- [x] Python: extend YAML config_loader with `dismissed_errata` (url + optional sha256)
- [x] Python: `ingest.discovery_meta` module exposing plugin discovery + YAML errata + dismissals as JSON
- [x] TS scrape module: fetch HTML, extract anchors, classify candidates
- [x] TS state module: per-handbook JSON read/write under `<cache>/discovery/handbooks/<slug>.json`
- [x] TS run/orchestrator: runDiscoverErrata + freshness gate + `--force`, `--dry-run`, `--doc=`, `--cache-root=`
- [x] TS pending report: `_pending.md` markdown writer with DRS link per candidate
- [x] TS GitHub: idempotent `gh issue create` per candidate (skip when `GH_TOKEN` not set; `--dry-run` short-circuits API call)
- [x] Wire `discover-errata` subcommand into `scripts/sources.ts` dispatcher + add banner for unreviewed
- [x] Piggyback: call discovery (in-process) at end of `bun run sources download`
- [x] Server hook: `apps/study/src/lib/server/discovery.ts` + extend `hooks.server.ts`
- [x] launchd plist + install/uninstall scripts at `tools/handbook-ingest/cron/`
- [x] Tests (53 ts + 9 py):
  - [x] python: dismissal field round-trip + validation
  - [x] ts: catalogue invariants + scrape classification + state merge transitions
  - [x] ts: freshness gate (stale + fresh + override window)
  - [x] ts: dismissal honored across runs
  - [x] ts: pending markdown + GH issue body shape
  - [x] ts: end-to-end run with stubbed fetch + meta
- [x] Update tasks.md phase status: R7 -> shipped
- [x] Append IDEAS.md follow-ups (SHA-256 dismissal wiring, DRS scrape)
- [x] YAML schema documented + dismissal placeholder added to all 3 configs
- [x] design.md grew a `Discovery dismissal` section ahead of the existing `Discovery state file` block
- [ ] Run `bun run check`, capture pre-existing-vs-mine deltas (final pass before PR)
- [ ] Open PR

## Notes

- pre-existing on `bun run check` (NOT mine):
  - libs/sources/src/regs/xml-walker.ts -- missing `fast-xml-parser` dependency
  - libs/ui/src/handbooks/AmendmentPanel.svelte + ErrataEntry.svelte -- 4 theme-lint violations (raw-duration, unknown tokens)
  - libs/ui/__tests__/{ErrataEntry,AmendmentPanel}.svelte.test.ts -- biome unused import + organizeImports
- Per WP+TODO style: do not fix pre-existing issues; note them in PR body.
