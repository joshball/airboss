---
title: 'TODO: Evidence Kind Data Layer build'
date: 2026-05-03
branch: worktree-agent-ac875cb3d5ce32e5b
worktree: .claude/worktrees/agent-ac875cb3d5ce32e5b
wp: docs/work-packages/evidence-kind-data-layer/
---

# TODO: Evidence Kind Data Layer build

Single-branch phased build of `evidence-kind-data-layer` per `tasks.md`. All commits land on the worktree's feature branch; one PR at end. Skill: `/ball-wp-build`.

## Phase 0 -- Pre-flight (verified)

- [x] Migration baseline confirmed: `drizzle/` has `0000_initial.sql` + `0001_hangar_invitation.sql`.
- [x] `drizzle/README.md` confirms `db push` is the runtime apply path.
- [x] WP B's three shims at `mastery.ts:284,289,293` are the targets.
- [x] No existing `teaching_exercise` references; no existing `card.kind` consumers; `assessment_methods` only mentioned in `mastery.ts` shims.
- [x] Memory/new + memory/[id] forms own card editing (study app, not hangar). Same for reps/new + reps/[id] for scenarios.
- [x] PreviewItemsPanel.svelte has `Record<SessionItemKind, ...>` -- needs new `teaching-exercise` entries in Phase 1.

## Phase 1 -- Constants + types

- [ ] Add `CARD_KINDS`, `CardKind`, `CARD_KIND_VALUES`, `CARD_KIND_LABELS` to `libs/constants/src/study.ts`.
- [ ] Add `SCENARIO_DEFAULT_ASSESSMENT_METHODS` constant.
- [ ] Extend `SESSION_ITEM_KINDS` with `TEACHING_EXERCISE: 'teaching-exercise'`.
- [ ] Re-export `CardKind` (and `TeachingExerciseRow`-related exports later) from `libs/types/src/index.ts`.
- [ ] Add `generateTeachingExerciseId` to `libs/utils/src/ids.ts` with prefix `texr`.
- [ ] Update `PreviewItemsPanel.svelte` `KIND_DEFINITIONS`, `KIND_LABELS`, `KIND_HELP` for new kind.
- [ ] `bun run check` passes.
- [ ] Commit.

## Phase 2 -- Schema + migration

- [ ] Schema: `card.kind`, indexes, CHECK; `scenario.assessment_methods`; `teachingExercise` table; `sessionItemResult.teachingExerciseId` FK.
- [ ] `bunx drizzle-kit generate --name=evidence_kind_data_layer` -> `0002_evidence_kind_data_layer.sql`.
- [ ] Hand-audit SQL.
- [ ] `bun run db reset --force`.
- [ ] Verify in psql.
- [ ] Commit.

## Phase 3 -- BC

- [ ] `cards.ts` createCard / updateCard: accept `kind`. Validation. `getCardsForNodeByKind`. `InvalidCardKindError`.
- [ ] `scenarios.ts` createScenario: accept `assessmentMethods`. Validation. `getScenariosForNodeByMethod`. `InvalidAssessmentMethodError`.
- [ ] New `teaching-exercises.ts` BC.
- [ ] `validation.ts`: extend zod schemas; add `newTeachingExerciseSchema`.
- [ ] `seed-cards.ts`: parse + pass `kind`.
- [ ] `seed-content.ts`: extend `SeedScenario` with `assessmentMethods?`.
- [ ] `seed-abby.ts`: pass through.
- [ ] Unit tests for validators + teaching-exercise CRUD.
- [ ] Commit.

## Phase 4 -- Authoring + audit + BC CHECK

- [ ] `apps/study/src/routes/(app)/memory/new` + `memory/[id]`: card kind select.
- [ ] `apps/study/src/routes/(app)/reps/new` + `reps/[id]`: scenario assessment-methods multi-checkbox.
- [ ] `scripts/db/check-card-kinds.ts`, `scripts/db/check-scenario-assessment-methods.ts`.
- [ ] `scripts/db.ts`: extend CHECK_TARGETS dispatcher.
- [ ] BC-level CHECK on session_item_result: `(item_kind = 'teaching-exercise') = (teaching_exercise_id IS NOT NULL)`.
- [ ] `bunx drizzle-kit generate --name=session_item_teaching_exercise_check` -> `0003_*.sql`.
- [ ] Run audit scripts; sanity-check output.
- [ ] Commit.

## Phase 5 -- Mastery wire-up

- [ ] Replace 3 shim queries with real partitions: cards by `(node_id, kind)`; reps by `LATERAL UNNEST(assessment_methods)`; teaching via `teaching_exercise.node_id` join.
- [ ] Replace file header documentation block.
- [ ] Update `mastery.test.ts` cases that asserted shim values; add new partition test cases per `test-plan.md`.
- [ ] Grep verification.
- [ ] Commit.

## Phase 6 -- Verification + final review + PR

- [ ] `bun run check` 0/0.
- [ ] `bun test` green.
- [ ] `bun run db reset --force` clean.
- [ ] `bunx drizzle-kit check` clean.
- [ ] Audit scripts run.
- [ ] Update WP frontmatter (status: shipped, review_status: pending then done after fix-pass).
- [ ] Final review pass; fix everything.
- [ ] Open PR.
