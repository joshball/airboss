# 2026-04-28 -- WP cert-syllabus PR 4 of N

Three remaining items from PR #264's tasks.md scope (phases 18-20 of
the WP). Companion to PR #229 (spec), PR #245 / #248 / #254 / #264.

## Tasks

- [x] Phase 22a -- Gate A: live-run `scripts/db/build-relevance-cache.ts`
      against the dev DB. Confirm counts match the dry-run manifest from
      PR #264 (7 nodes, 28 leaves, 12 links). Verify idempotency by
      re-running.
- [x] Phase 22b -- Gate B: ship `scripts/db/strip-authored-relevance.ts`
      that walks `course/knowledge/**/node.md` and removes any
      `relevance:` frontmatter key. Run it; current authoring has no
      such key, so 0 files modified is expected and ships the script
      as future-proofing.
- [x] Phase 22c -- ship `scripts/db/migrate-study-plans-to-goals.ts`.
      Walks every `study_plan` row, materialises one Goal per plan
      with linked GoalSyllabus rows pointing at the credentials'
      primary syllabi. Idempotent via `study_plan.goal_migrated_at`
      and `goal.seed_origin` markers. Skip certs whose credential
      has no primary syllabus seeded yet.
- [x] Vitest coverage for the migration: empty cert_goals, single
      cert, multi-cert, cert-with-no-syllabus-seeded, idempotency.
- [x] Gate C: `bun run check` clean.
- [x] Push branch + open PR.
