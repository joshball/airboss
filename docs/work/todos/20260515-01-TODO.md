# 2026-05-15 -- Session todo

DateTime: 2026-05-15
Machine: darwin
Branch: main (orchestrator)
Context: Post Phase 2 (`:::phase` directive, PR #991 merged). User asked to
clear the NOW.md follow-up menu in parallel worktrees.

## Done this session

- [x] Phase 2 -- `:::phase` directive replaces hardcoded-H2 phase splitter (PR #991, merged d76b33d6)

## In flight (dispatched in parallel worktrees)

- [ ] Track A -- Phase 3 directive cleanup: audit `:::chart`/`:::scenario`/`:::xc-viewer`
      for the H2-eats-content smell + `:::callout` registry gap, then ship all three
      (`:::callout` formalisation, `:::cards` -> nested-markdown-body migration,
      whatever the audit surfaces on chart/scenario/xc-viewer). Stop and ask if a
      sub-item has no clear right answer.
- [ ] Track B -- draft four manual-test walkthrough scaffolds: `wx-engine`,
      `course-tree-arbitrary-depth`, `card-question-tier`, `weather-comprehensive`.
      Scaffold only; Joshua runs the manual test and signs off.
- [ ] Track C -- hangar-review-queue hydration leak / `hangar.docs_search_index`
      Postgres crash: investigate, root-cause, fix, ship PR (or BLOCKER).
- [ ] Track D -- WP sign-off review summaries for `course-reader-and-editor` and
      `xc-viewer-v1`. Agent produces the review summary; Joshua signs off
      (`human_review_status` -- agents are lint-blocked from writing it).

## Notes

- Track A: user said "do all three" but "stop and ask if you don't know the
  right thing." Audit runs first; refactors follow within the same worktree.
- Track B walkthroughs are owed per NOW.md "Walkthroughs owed" list.
- Track D cannot be closed by an agent -- sign-off is human-only.
