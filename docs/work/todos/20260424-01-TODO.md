# 2026-04-24 -- out-of-scope findings

Session context: fixing `scripts/references/scan.ts` template-literal
miscount bug in an isolated worktree. Noted the items below while
running `bun run check` and `bun run test` on the branch tip; they
reproduce on `origin/main` (confirmed via `git stash` round-trip), so
none are caused by this PR.

## Findings

- `bun scripts/references/validate.ts` fails at startup with
  `Cannot find module '@ab/constants'` when resolving
  `apps/study/src/lib/help/content/**.ts`. `bun run check` surfaces
  this as a top-level error. Either the help-content scan needs a
  per-app resolver config or the files should import via a pathway
  bun can resolve from the repo root. Repro: `bun run check`.
- 10 test files fail on `origin/main` today:
  - `libs/bc/study/src/{calibration,dashboard,knowledge,knowledge.cert,knowledge.progress,plans,scenarios}.test.ts` -- all fail to load (likely the same `@ab/constants` / workspace resolution issue; needs a closer look).
  - `libs/themes/__tests__/typography-packs.test.ts` -- `study/flightdeck references airboss-compact` expectation no longer holds.
  - `tools/theme-codemod/__tests__/transforms.test.ts` -- `handles a realistic Svelte style block` snapshot drift.
  - `tools/theme-lint/__tests__/rules.test.ts` -- `passes legacy --ab-* aliases (they exist)` expects 0 unknown-token hits, now sees 1.
  Total: 3 assertion failures + 7 load failures. Full counts: 45 files / 757 tests, 10 files failed, 3 tests failed, 1 skipped.

Not fixing in this PR. Capturing so the next session or the
responsible work packages can route them.
