# Sources backend criticals + open-corpus enumeration

Branch: `fix/sources-backend-criticals`
Worktree: `.claude/worktrees/agent-a4bb10d9c7e5a4968`

## Findings

- [x] (1) ENUMERATED_CORPORA closed enum -> drive enumeration off the resolver registry (live iterable view; new corpora register themselves)
- [x] (2) `runIngestCli` (ac/acs/aim) returns 0 even when `skipReasons` populated -> classify hard vs soft skips; new exit-code constants (`INGEST_EXIT_CODES` in `libs/sources/src/shared/exit-codes.ts`)
- [x] (3) No SHA verification on register paths -> shared `verifyCachedSha` helper; `--skip-sha-verify` opt-out only
- [x] (4) Lifecycle promotion non-atomic with SOURCES/EDITIONS mutations -> new `commitIngestBatch` helper validates before mutating, rolls back on failure; AC/ACS/AIM/handbooks/regs/bootstrap migrated
- [x] (5) `runSnapshotCli` writes empty data -> hydrate via `hydrateRegsFromDerivatives`; new `writeSnapshotData(path, data)` helper; single generation per CLI invocation
- [x] Tests: registry-driven enumeration (CR-08, CR-09); commit batch atomicity (CB-01..04); SHA mismatch (SHA-01..04); exit-code classification (EX-01..08); snapshot hydration (S-08, S-09); AC CLI hard-skip exit (CLI-01..06)
- [x] `bun run check` clean for changed lib files (pre-existing fast-xml-parser dep issue, not from these changes)
- [ ] Stage, commit, push, PR
