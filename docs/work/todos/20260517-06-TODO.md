# 2026-05-17 -- Foundation libraries second-pass review

Independent second 10x-style review of the eight shared-foundation libraries
(`constants`, `ui`, `themes`, `auth`, `db`, `types`, `utils`, `audit`) to catch
what PR #1050's first pass missed.

## Findings

- [x] F1 -- `renderMarkdown` double-escapes `&`/`<`/`>` in link URLs (major, correctness)
- [x] F2 -- `Checkbox` derives `id` from label text -> duplicate-id collision (major, a11y, convergent)
- [x] F3 -- `FormField` derives `id`/`describedBy` from label text -> collision (major, a11y, convergent)
- [x] F4 -- `InfoTip` derives `id`/`titleId` from term text -> duplicate-id (major, a11y, convergent)
- [x] F5 -- `MetricExplainer` derives `id` from label text -> duplicate-id (major, a11y, convergent)
- [x] F6 -- `PanelShell` derives `<h2>` id from title text -> duplicate-id (minor, a11y, convergent)
- [x] F7 -- `redactSensitive` returns the live unredacted object on cycle (minor, security)
- [x] F8 -- Dialog close glyph not `aria-hidden`, inconsistent with Drawer (nit, a11y)

## Verification

- [x] `bun run check branch` -- 0 errors / 0 warnings
- [x] Relevant unit tests pass
- [x] Re-grep each symptom -> gone
</content>
