---
date: 2026-05-08
session: hangar-ingest-review-queue build
branch: feat/hangar-ingest-review-queue
wp: docs/work-packages/hangar-ingest-review-queue/
---

# Session todos -- hangar-ingest-review-queue build

Ship the work package end to end. Follows the phased plan in
`docs/work-packages/hangar-ingest-review-queue/tasks.md`.

## Phase 1: schema + BC scaffold

- [x] `libs/bc/ingest-review/` scaffold with package.json, runtime barrel, server barrel, schema, queries, types
- [x] `libs/constants/src/ingest-review.ts` with KIND / ACTION / STATUS / CORPUS literals
- [x] tsconfig path alias + drizzle.config.ts + scripts/check-browser-globals.ts entries
- [x] Drizzle migration `drizzle/0002_ingest_review.sql` (two tables, indexes, CHECKs, FK cascades)

## Phase 2: plugin registry + producer

- [x] `plugin.ts` registry with registerPlugin / getPlugin / listPlugins / reset
- [x] `producer.ts` runProducers helper with per-plugin error capture + stale flip
- [x] `scripts/ingest-review/run-producers.ts`
- [x] `scripts/ingest-review/import-overrides.ts`
- [x] `scripts/ingest-review/export-overrides.ts`
- [x] `scripts/ingest-review/yaml-sidecar.ts` (serialize / parse) with byte-stable output

## Phase 3: caption-orphan plugin

- [x] `plugins/handbook-shared.ts` warnings.json + manifest.json reader
- [x] `plugins/handbook-caption-orphan.ts` produceIssues / findCandidates / validateAction / serializeForYaml
- [x] `plugins/index.ts` registration barrel (idempotent)

## Phase 4: image-orphan plugin

- [x] `plugins/handbook-image-orphan.ts` mirror plugin
- [x] Action set restricted to `pair / mark-extraneous / mark-decorative`

## Phase 5: hangar UI

- [x] Routes: `/ingest-review` (queue) + `/ingest-review/[issueId]` (detail) with 7 form actions
- [x] Components: `OrphanCard` (inline in [issueId]/+page.svelte), `CandidateStrip`, `StatusBadge`, `pdf-link.ts`
- [x] Filter chips on status / corpus / kind / source via QUERY_PARAMS
- [x] `Run producers` form action on the queue page
- [x] Nav.svelte link added

## Phase 6: YAML round-trip + integration

- [x] `tools/handbook-ingest/ingest/overrides_loader.py` reader
- [x] `tools/handbook-ingest/ingest/normalize.py` filters warnings via sidecar at write_outputs
- [x] `tools/handbook-ingest/tests/test_overrides_loader.py` parser tests
- [x] Sample sidecar `scripts/sources/config/handbooks/ifh-overrides.yaml` (empty placeholder)
- [x] `docs/products/hangar/PRD.md` row added

## Tests

- [x] `libs/bc/ingest-review/src/schema.test.ts` (Drizzle table-shape)
- [x] `libs/bc/ingest-review/src/plugin.test.ts` (registry)
- [x] `libs/bc/ingest-review/src/plugins/handbook-shared.test.ts`
- [x] `libs/bc/ingest-review/src/plugins/handbook-caption-orphan.test.ts`
- [x] `libs/bc/ingest-review/src/plugins/handbook-image-orphan.test.ts`
- [x] `scripts/ingest-review/yaml-sidecar.test.ts`
- [x] `tools/handbook-ingest/tests/test_overrides_loader.py`

## Verification

- [x] `bun run check branch` passes (biome, browser-globals, svelte-check:study)
- [x] `bun run check` (full hangar svelte-check) clean -- queued for full run before PR
- [x] `bunx vitest run libs/bc/ingest-review/ scripts/ingest-review/` -- 54 / 54 pass
- [ ] Manual test plan walked by Joshua (in PR description)

## Punted to later

- Decision: the 21 live caption-orphans are deferred as the queue's first work item AFTER it ships.
  Per affirmed scope, this build does NOT manually fix YAML; the queue is the surface.
- Drizzle snapshot drift in `drizzle/meta/0001_snapshot.json` is pre-existing (study app + audit log
  schema have moved past it). The new migration file (0002_ingest_review.sql) only contains the
  two new tables; full `drizzle generate` produces a kitchen-sink migration that re-creates every
  drifted table. Dev flow uses `bun run db push` against the schema directly, which is what the
  team already does. Surfaced in PR description; not blocking.
