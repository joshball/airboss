---
date: 2026-04-27
fix_status: in-progress
review_set: 2026-04-27 full-codebase 12-axis
total_issues: 183
critical: 4
major: 62
minor: 82
nit: 34
---

# 2026-04-27 Full-Codebase Fix Plan

Consolidated fix plan for 12 reviews (correctness, security, perf, architecture, a11y, patterns, testing, dx, ux, svelte, backend, schema). Fixes everything per CLAUDE.md "ALWAYS FIX EVERYTHING from a review" rule. Convergent findings get a single root-cause fix. Token / pattern pass runs LAST.

## Cluster execution order

1. **Security/A11y/Svelte criticals** -- token leak, ConfirmAction outline (convergent: a11y + svelte).
2. **Schema** -- knowledge_node.lifecycle decision, audit/hangar enum CHECKs, scenario_option promotion, FK fixes, indexes. Single migration.
3. **Backend** -- hangar BC extraction, sim attempt Zod, Drizzle in routes, secure cookies, upload temp cleanup, isRedirect, login redirect codes.
4. **Correctness** -- snooze status filter, calibration cumulative, /memory/review load-write, slot ownership, parseLastAmended, plans null guards, addSkipNode eligibility (or comment), recordItemResult assertion, getReplacementCard limit.
5. **Security/Backend remainders** -- upload basename, markdown escapeAttr, heartbeat upper bound, auth rate limit, thumbnail/download path guards, applySecurityHeaders log.
6. **A11y** -- MapPanel labels, handbook focus, popover scrim, skip link z-index, route inputs, native nav menus, confidence radios, aria-haspopup, instrument SVGs, dialog warn, TextField textarea, ConfidenceSlider id, h2 skip, knowledge stepper glyphs, role="status" cleanup, banner labels, NavIndicator, identity menu, share preview, reduced-motion.
7. **Perf** -- citation picker indexes, /handbooks N+1, /sources/files parallel + lazy preview, knowledge browse, fetchCardCandidates predicate, recoverOrphanedRunning batch, getRecentActivity widening, getDashboardStats streak dedupe, getDomainBreakdown sql, pdf extract async note.
8. **UX** -- hangar source ops feedback, handbook empty CLI, help wp identifier, hangar user banner, knowledge "Got it" status, em-dash + interpunct, sources buttons grouping, calibration empty copy, calibration CTA copy, memory rename hint, memory continue glyph, plans archived, memory new banner, session start preset, sessions progressbar, memory review caught up, knowledge node lead, hangar jobs live badge, login dev panel, sim retry busy, breadcrumb, sim debrief stale, plans active badge.
9. **Svelte** -- ConfirmAction outline (already in 1), .svelte.ts -> .ts renames, HelpSearchPalette tokens, layout $derived sync, untrack effects, timer cleanup, focus trap reuse, onDestroy.
10. **Testing** -- BC tests for cards/snooze/stats/reviews/validation/handbook-validation/feedback/formatters/card-cross-references/auth(cookies+logout+email)/render-modes; smoke H1, weak asserts, lower-bound tests, fresh-user pattern, delete skipped tests, vi.mock ordering, isolate as any, ui toBeTruthy, validateSyllabusTree return, dependency seam.
11. **DX** -- error cause chains, README, single-letter rename, "not a file" paths, env.example, setup script password, CLI error messages, logger, ensureSvelteKitSync, colors helper.
12. **Architecture** -- hangar BC + schemas + study reference page (deferred WP if too large); citations + bc-study coupling; lib/db drop @ab/auth dep; CLAUDE.md tree update; libs/types and libs/activities deps; bc-citations studySchema dedupe; bc-sim persistence export; UI scattered components doc; libs/sources naming; sim instruments lib (deferred).
13. **Patterns / tokens (LAST, alone)** -- 59 font-size literals (libs/help, libs/aviation, CrosswindComponent, references dev page), QUERY_PARAMS additions, /glossary route constant, Three.js horizon tokens, rgba shadows, transition timing, email templates constants, dev page hex, relative cross-lib import, as any -> as unknown, lenses sql helper.

## Deferred work packages

Architecture review flagged structural items too big for this PR. Each becomes a numbered work package:

- `extract-hangar-bc` -- create `libs/bc/hangar/`, move ~2400 lines from `apps/hangar/src/lib/server/`. Trigger: after this PR merges. *Phase 1 minimal extraction lands in this PR (server-side modules already absorb route Drizzle queries); full BC migration deferred.*
- `bc-citations-coupling` -- decide: own `libs/bc/citations` BC or fold into bc-study. Trigger: next review of citation surface.
- `route-style-extraction` -- promote recurring shells (empty-state, score-card, panel-header, ...) from 27 oversized route `<style>` blocks into `libs/ui/`. Trigger: next major UI overhaul.
- `extract-sim-instruments` -- move sim instruments to a shared lib. Trigger: when avionics/ app is created.

## Token migration scope

Deferred from semantic clusters into the patterns/tokens pass at the end. Files:

- libs/help/src/ui/* (8 files, ~30 font-size literals)
- libs/aviation/src/ui/* (4 files)
- libs/activities/src/crosswind-component/CrosswindComponent.svelte (px font-size + literal transition)
- apps/study/src/routes/(dev)/references/+page.svelte (hex fallback + em font-size)
- libs/auth/src/email/templates.ts (hex + px in email HTML, behind EMAIL_BRAND constant)
- libs/ui popovers (3) and aviation reference primitives (2): rgba shadow drift
- apps/sim/src/lib/horizon/Horizon3D.svelte (Three.js scene colors)
