---
session: 20260517-212403-79769
date: 2026-05-17
branch: main
type: unfinished
items: 6
---

# Unfinished work -- integration coverage sweep rework

The session reworked `bun run test integration`. PR #1056 (postgres leak fix)
merged. Three PRs remain open and one branch is un-PR'd; one hard blocker.

### Merge Track B -- spec sampling + resume

- **What**: PR #1051 -- spec changes (sampling, tier filter, resume, manifest).
- **Where**: PR #1051, branch `worktree-agent-a13f6703208f7c193`.
- **Why deferred**: parent working tree was dirty with another agent's
  uncommitted changes; merge train stopped to avoid clobbering it.
- **Next action**: survey `git status` / `git stash list`, then
  `gh pr merge 1051 --squash --delete-branch`.
- **Severity**: high

### Merge Track C -- reporter + live UI

- **What**: PR #1057 -- reporter tree-grouping, live progress dashboard,
  crash detection.
- **Where**: PR #1057, branch `track-c/integration-reporter-rework`.
- **Why deferred**: same dirty-working-tree halt as Track B.
- **Next action**: `gh pr merge 1057 --squash --delete-branch`.
- **Severity**: high

### Land Track A -- server + build cache + dispatcher

- **What**: server prod-build, build cache, `integration` sub-dispatcher.
  Pushed but never PR'd.
- **Where**: branches `track-a/integration-sweep-build-cache` (commit `0e611f68`)
  and `track-a/integration-sweep-prod-server` (`b4edd30d`) -- TWO track-a
  branches exist; reconcile which is current before PR'ing.
- **Why deferred**: blocked behind the prod-server boot failure below.
- **Next action**: pick the correct track-a branch, fix the `node build` ->
  `bun ./build/index.js` issue in `playwright.config.ts`, open a PR, merge.
- **Severity**: high

### BLOCKER -- production build does not boot (zod/better-auth mismatch)

- **What**: `bun ./apps/flightbag/build/index.js` crashes on boot:
  `z.coerce.boolean(...).meta is not a function`. Repo-wide zod / better-auth
  version skew -- also breaks `apps/study`'s production build.
- **Where**: no single artifact -- root `package.json` catalog vs `better-auth`'s
  zod peer dependency.
- **Why deferred**: discovered late; out of the test-sweep file scope; needs a
  dependency-version investigation.
- **Next action**: align the zod version; verify both `apps/flightbag` and
  `apps/study` build AND boot via `bun ./build/index.js`. This is the gate for
  Track A's prod-build server.
- **Severity**: critical

### `node build` -> `bun ./build/index.js`

- **What**: Track A's integration webServer `command` uses `node build`. This is
  a Bun shop (CLAUDE.md: always bun, never node).
- **Where**: `playwright.config.ts` on a `track-a/*` branch, integration
  webServer entry.
- **Why deferred**: spotted during wind-down review, not yet corrected.
- **Next action**: change to `bun ./build/index.js` before PR'ing Track A.
- **Severity**: medium

### PR #1043 superseded -- close it

- **What**: PR #1043 was the first WIP reporter slice; PR #1057 supersedes it.
- **Where**: PR #1043, branch `feat/integration-sweep-output`.
- **Why deferred**: left open when the work was split into 3 tracks.
- **Next action**: confirm #1057 fully covers it, then close #1043 and delete
  `feat/integration-sweep-output`.
- **Severity**: low
