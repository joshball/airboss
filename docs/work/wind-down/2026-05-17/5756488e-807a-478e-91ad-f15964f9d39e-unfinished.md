---
session: 5756488e-807a-478e-91ad-f15964f9d39e
date: 2026-05-17
branch: main
type: unfinished
items: 4
---

# Unfinished work -- zod alignment + integration sweep + branch triage

Session shipped 3 PRs (all merged to main): #1064 zod 3->4 alignment (fixes the
prod-build boot crash), #1065 `integration list` exits 0, #1067 deepInk contrast
tokens (38 skips cleared). Also triaged 4 stale branches: 2 verified-stale and
deleted (#1066, #1068 closed), 1 rebased + merged (#1067). The items below were
not finished or were found mid-session and punted.

## `integration --full` not verified green

- **What**: `bun run test integration --full` (~3484-URL prod-build sweep, the
  original crash repro) has not had a clean uninterrupted pass. Attempts were
  interrupted -- first a port conflict from orphan webServer processes, then the
  final background run was killed (SIGTERM to the DB pool; `last-run.json`
  showed `wallMs: 15`, `total: 0`, `status: failed`).
- **Where**: `bun run test integration --full`; artefacts at
  `tests/integration/.out/last-run.json` + `coverage-summary.json`.
- **Why deferred**: session ended before a clean run completed. The default
  209-URL sample sweep passes cleanly, proving the prod server + zod fix work
  under load -- but the full run is the brief's explicit gate.
- **Next action**: `pkill -f 'build/index.js'; pkill -f playwright`, confirm
  port 9647 free, then run `bun run test integration --full` to completion and
  confirm it survives without the server OOM-crashing.
- **Severity**: high

## `@ab/sources/regs/ingest` subpath export missing (broken test)

- **What**: `scripts/sources/register.test.ts` fails -- `Cannot find package
  '@ab/sources/regs/ingest'`. PR #1056 changed `scripts/sources/register/cfr.ts`
  to import `@ab/sources/regs/ingest`, but `libs/sources/package.json` `exports`
  only declares `.` and `./server` -- no `./regs/ingest` entry.
- **Where**: `libs/sources/package.json` exports map;
  `scripts/sources/register/cfr.ts:15`; `libs/sources/src/regs/ingest.ts` (the
  target file exists).
- **Why deferred**: pre-existing breakage from merged sibling PR #1056,
  unrelated to this session's zod work.
- **Next action**: add a `"./regs/ingest"` (and likely `"./regs"`) entry to the
  `exports` map in `libs/sources/package.json` pointing at
  `./src/regs/ingest.ts`; re-run `scripts/sources/register.test.ts`.
- **Severity**: high

## `14cfr14` missing CFR section rows (failing seed-shape test)

- **What**: `scripts/db/seed-references-from-manifest.test.ts` fails -- "every
  CFR reference with sections has at least one top-level section row", missing
  `14cfr14`.
- **Where**: `scripts/db/seed-references-from-manifest.test.ts:1456`.
- **Why deferred**: missing-source-corpus / environment-dependent; the test
  message says to run `bun run sources register cfr --title=14 --edition=<date>`
  then re-seed. Unrelated to zod. Not investigated.
- **Next action**: determine whether Title 14 CFR Part 14 should be in the
  registered corpus; if so register + re-seed it, otherwise scope the test to
  the parts actually present.
- **Severity**: medium

## `ball-worktree/guard.sh` syntax error blocked all tool calls

- **What**: the `ball-worktree` PreToolUse guard hook
  (`~/src/_me/ai/agent-skills/skills/worktree/ball-worktree/guard.sh`) crashed
  on a bash syntax error at line 331 -- `unexpected token ';&'`. Because it's a
  PreToolUse hook it blocked EVERY Bash and Read call until it was fixed
  out-of-band. `;&` is a `case`-fallthrough operator, invalid inside a `[[ ]]`
  conditional.
- **Where**: `~/src/_me/ai/agent-skills/skills/worktree/ball-worktree/guard.sh:331`
  (outside this repo -- a shared skills repo).
- **Why deferred**: not an airboss-repo file; was fixed out-of-band mid-session
  so work could continue, but the root cause should be confirmed fixed.
- **Next action**: verify line 331 of guard.sh is correct (likely `;&` should be
  `;;`, or a misplaced `[[`/`case` boundary). Confirm the hook parses cleanly
  with `bash -n guard.sh`.
- **Severity**: medium

## Note -- not unfinished, just FYI

- PR #844's rich-reader feature (`wp-flightbag-rich-reader`) is on main but the
  work package is still `status: draft` with pending human review, and
  docs/work/NOW.md flags that the shipped rich-reader code has never been loaded
  in a real browser. Browser verification of that surface is owed -- tracked in
  NOW.md, not introduced by this session.
