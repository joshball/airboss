---
session: 20260517-222752-41597
date: 2026-05-17
branch: main
type: unfinished
items: 5
---

# Unfinished work -- integration-test-sweep rework

Session shipped PR #1064 (zod 3->4 alignment, fixes the prod-build boot crash)
and PR #1065 (`integration list` exits 0). Both merged to main. The items below
were not finished or were found mid-session and punted.

## `integration --full` not verified green

- **What**: `bun run test integration --full` (~3484-URL prod-build sweep, the
  original crash repro) has not had a clean uninterrupted pass. Every attempt
  this session was interrupted -- first by a port conflict from orphan
  webServer processes, then the final background run was killed (SIGTERM to the
  DB pool; `last-run.json` shows `wallMs: 15`, `total: 0`, `status: failed`).
- **Where**: `bun run test integration --full`; artefacts at
  `tests/integration/.out/last-run.json` + `coverage-summary.json`.
- **Why deferred**: session ended before a clean run completed. The default
  209-URL sample sweep passes cleanly, which proves the prod server + zod fix
  work under load -- but the full run is the brief's explicit gate.
- **Next action**: kill all stale `build/index.js` / `playwright` procs
  (`pkill -f 'build/index.js'; pkill -f playwright`), confirm port 9647 free,
  then run `bun run test integration --full` to completion and confirm it
  survives without the server OOM-crashing.
- **Severity**: high

## `@ab/sources/regs/ingest` subpath export missing (broken test)

- **What**: `scripts/sources/register.test.ts` fails -- `Cannot find package
  '@ab/sources/regs/ingest'`. PR #1056 changed `scripts/sources/register/cfr.ts`
  to import `@ab/sources/regs/ingest`, but `libs/sources/package.json` `exports`
  only declares `.` and `./server` -- no `./regs/ingest` entry.
- **Where**: `libs/sources/package.json` exports map;
  `scripts/sources/register/cfr.ts:15`; `libs/sources/src/regs/ingest.ts` (the
  target file exists).
- **Why deferred**: pre-existing breakage from merged sibling PR #1056,
  unrelated to this session's zod work. Not in scope of either PR shipped here.
- **Next action**: add a `"./regs/ingest"` (and likely `"./regs"`) entry to the
  `exports` map in `libs/sources/package.json` pointing at
  `./src/regs/ingest.ts`; re-run `scripts/sources/register.test.ts`.
- **Severity**: high

## `14cfr14` missing CFR section rows (failing seed-shape test)

- **What**: `scripts/db/seed-references-from-manifest.test.ts` fails -- "every
  CFR reference with sections has at least one top-level section row", missing
  `14cfr14`.
- **Where**: `scripts/db/seed-references-from-manifest.test.ts:1456`.
- **Why deferred**: missing-source-corpus / environment-dependent; the test
  message says to run `bun run sources register cfr --title=14 --edition=<date>`
  then re-seed. Unrelated to zod. Not investigated.
- **Next action**: determine whether Title 14 CFR Part 14 should be in the
  registered corpus; if so register + re-seed it, otherwise scope the test to
  the parts that are actually present.
- **Severity**: medium

## `bun run check branch` not run on the merged end-state

- **What**: brief asked for `bun run check branch` clean as the end gate. Both
  PRs passed `check dirty` individually pre-merge, but a final `check branch`
  against merged `main` was not run.
- **Where**: repo root, `bun run check branch`.
- **Why deferred**: session ended before the final verification pass.
- **Next action**: from a clean worktree on latest `main`, run
  `bun run check branch` and confirm 0 errors / 0 warnings.
- **Severity**: medium

## My `zod-v4-align` worktree not removed

- **What**: worktree `.claude/worktrees/zod-v4-align` (detached at the merged
  main commit) is still on disk. It held the full-sweep process so it could not
  be removed during the session.
- **Where**: `.claude/worktrees/zod-v4-align`.
- **Why deferred**: full sweep was running inside it; removing it would have
  killed the run.
- **Next action**: the sweep process is done -- the worktree is detached HEAD,
  no branch, no unpushed commits. Safe to remove:
  `git worktree remove -f -f .claude/worktrees/zod-v4-align`. (Note: it has a
  local `.env` copy -- gitignored, lost on removal, which is fine.)
- **Severity**: low

## Note -- not mine, flagged for the user

- A separate agent has a worktree `agent-a852f59379803cfa3` on branch
  `chore/zod-4-upgrade` -- a parallel zod 4 effort. PR #1064 already landed the
  zod 4 alignment on main, so that branch is likely now redundant or will
  conflict. Left untouched (not this session's worktree). The user should
  decide whether to close/redirect it.
