---
session: 20260517-163554-26560
date: 2026-05-17
branch: main
type: unfinished
items: 5
---

# Unfinished work -- session 20260517-163554-26560

All code/docs work this session shipped and merged (8 PRs). The unfinished items
are user-only decisions and one follow-on plan phase. Nothing is blocked on an agent.

## course-reader-and-editor sign-off

- **What**: WP `course-reader-and-editor` -- `human_review_status` is still `pending`.
  The deferred `/ball-review-full` ran this session (105 findings, all fixed in
  PR #1042). The WP is now review-clean and ready to sign off.
- **Where**: `docs/work-packages/course-reader-and-editor/spec.md` frontmatter.
- **Why deferred**: sign-off is human-only; the user explicitly reserved
  `human_review_status` and did not flip it this session.
- **Next action**: user walks the feature (walkthrough file has the steps), then
  an agent runs `bun run wp set course-reader-and-editor human-review signed-off`
  + `status signed-off` and ships the one-line frontmatter change as a small PR.
- **Severity**: medium

## xc-viewer-v1 sign-off

- **What**: WP `xc-viewer-v1` -- `human_review_status` still `pending`. User said
  "looks good" on the spec; the port-collision blocker was fixed (PR #1017).
- **Where**: `docs/work-packages/xc-viewer-v1/spec.md` frontmatter.
- **Why deferred**: sign-off is human-only; not flipped this session.
- **Next action**: `bun run wp set xc-viewer-v1 human-review signed-off` +
  `status signed-off`, shipped as a small frontmatter PR. Then `/ball-wp-build`
  can run the WP.
- **Severity**: medium

## Retention-bearing callouts -- Phase B + C

- **What**: the retention-bearing-callouts plan (`docs/work/plans/2026-05-retention-bearing-callouts.md`)
  has Phase A done (PR #1016). Phase B (path-scoped build validator rule requiring
  `:::cards` in study-content `:::tip`/`:::warn`) and Phase C (author-guidance doc)
  are not built.
- **Where**: `docs/work/plans/2026-05-retention-bearing-callouts.md`.
- **Why deferred**: scoped as future work; no backfill needed (study corpus has
  zero callouts today), so the rule can ship whenever.
- **Next action**: add the build-time validator rule scoped to `course/knowledge/**`,
  then update knowledge-authoring guidance.
- **Severity**: low

## 4 walkthrough scaffolds owed sign-off

- **What**: PR #1012 scaffolded 4 manual-test walkthroughs (wx-engine,
  course-tree-arbitrary-depth, card-question-tier, weather-comprehensive). They are
  scaffolds -- the user runs them and signs off the corresponding WPs.
- **Where**: `docs/work/walkthroughs/2026-05-16-*-walkthrough.md`.
- **Why deferred**: manual testing is a human action.
- **Next action**: user runs each walkthrough, then the WP can close.
- **Severity**: low

## Stale generated doc -- SHIPPED.md

- **What**: `docs/work/SHIPPED.md` is stale -- this session's merges are not
  reflected. `bun run track generate` refreshes it.
- **Where**: `docs/work/SHIPPED.md`.
- **Why deferred**: out of scope of any feature PR; a generated artifact.
- **Next action**: `bun run track generate`, commit the regenerated board.
- **Severity**: low
