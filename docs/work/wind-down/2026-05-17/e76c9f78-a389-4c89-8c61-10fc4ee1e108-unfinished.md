---
session: e76c9f78-a389-4c89-8c61-10fc4ee1e108
date: 2026-05-17
branch: main
type: unfinished
items: 8
---

# Unfinished work -- session e76c9f78

All this session's PRs merged to main. The items below are deferred
follow-ups, gated decisions, and tracked next-phases -- none is a loose
in-flight branch.

## ADR 028 awaits human approval

- **What**: ADR 028 (content-intent frontmatter) defines the Layer 2
  `content_status` + `intent` block contract for the content census.
- **Where**: `docs/decisions/028-*` -- status `proposed`.
- **Why deferred**: only the user can approve an ADR to `accepted`.
- **Next action**: user reviews + accepts ADR 028. Until then, Layer 2
  frontmatter authoring must not start at scale.
- **Severity**: high

## hangar-content-census WP awaits sign-off

- **What**: the `hangar-content-census` work package is `status: draft`.
- **Where**: `docs/work-packages/hangar-content-census/spec.md`.
- **Why deferred**: `human_review_status` can only be set by the user;
  the lint blocks an agent from touching it.
- **Next action**: user reviews the WP + sets `human_review_status`.
- **Severity**: medium

## Content census Phase 3 -- remaining corpora

- **What**: Phase 3 (depth: real gap view + next-list per corpus) is done
  for wx-catalog and knowledge-nodes only. 12 corpora still have honest
  Phase-3 placeholders for their gap/intent/next sections.
- **Where**: `docs/work-packages/hangar-content-census/tasks.md` Phase 3.
- **Why deferred**: breadth-first plan -- one corpus at a time, value-ranked;
  cards is next.
- **Next action**: continue Phase 3, starting with the cards corpus.
- **Severity**: medium

## Content census Layer 2 -- intent frontmatter authoring

- **What**: no corpus has Layer 2 content-intent frontmatter authored yet.
- **Where**: `docs/work-packages/hangar-content-census/tasks.md` Phase 3.
- **Why deferred**: gated on ADR 028 acceptance (see above).
- **Next action**: once ADR 028 is `accepted`, author the `intent` block
  per corpus, knowledge nodes first.
- **Severity**: medium

## wx-engine AIRMET text emitter

- **What**: the wx-engine has no AIRMET/SIGMET text emitter, so that whole
  product is structurally unmatchable by the catalog coverage matcher and
  cannot be drilled with generated content.
- **Where**: tracked in `docs/work-packages/hangar-content-census/tasks.md`
  "Known follow-ups"; surfaced on the `/content/wx-catalog` gap view.
- **Why deferred**: a real engine feature, out of scope for the catalog
  coverage work.
- **Next action**: design + build an AIRMET text emitter in `libs/wx-engine`.
- **Severity**: medium

## Catalog coverage -- 44 uncovered token families

- **What**: 44 of 87 METAR/TAF token families have no generated example
  because the wx-engine doesn't model the synoptic conditions that produce
  them (`+FC`, `VA`, `SQ`, wind-shear `WS`, `BECMG`, ...).
- **Where**: `/content/wx-catalog` gap view; WP tasks.md "Known follow-ups".
- **Why deferred**: each is a small incremental engine feature.
- **Next action**: prioritise + add engine features per token family.
- **Severity**: low

## frontal-pressure-march contributes 0 catalog examples

- **What**: the temporal pilot scenario `frontal-pressure-march` is not
  represented in the encoded-text catalog, so it contributes 0 coverage.
- **Where**: WP tasks.md "Known follow-ups"; wx-scenarios census.
- **Why deferred**: one authoring pass not yet done.
- **Next action**: author `frontal-pressure-march` products into the catalog.
- **Severity**: low

## Truth Model v2 -- fully shipped (no remainder)

- **What**: Truth Model v2 steps 1-7 are all merged (PRs #1001, #1009).
  Recorded here only to confirm nothing of that plan is outstanding.
- **Where**: `docs/work/plans/2026-05-14-truth-model-v2-temporal.md`.
- **Next action**: none -- the plan is complete.
- **Severity**: low
