---
session: 20260517-160100-94636
date: 2026-05-17
branch: main
type: unfinished
items: 4
---

# Unfinished work -- session 20260517-160100-94636

This session shipped 12 PRs (all merged) building the aviation weather products reference, the FAA source-corpus drift check, and citation verification. The items below are deliberately deferred, each with a recorded trigger -- not dropped work.

### Drift-check Step 3: hangar surface

- **What**: Surface the monthly source-corpus drift report inside the hangar app as a review-queue item (e.g. "AC 61-65 has revision K upstream; cache has J"), instead of only as a dated file in `docs/work/reviews/`.
- **Where**: planned in `docs/work/plans/2026-05-17-source-corpus-drift-check.md` (Step 3); no artifact yet.
- **Why deferred**: `apps/hangar/` is still a scaffold (not in active use); Steps 1-2 (the `bun run sources report` CLI + monthly job) already deliver the value to the developer. The hangar surface is polish, and the heaviest of the three steps.
- **Next action**: build it as a small PR when a content author other than the developer needs the drift signal (the recorded trigger).
- **Severity**: low

### feat/wx-examples-page -- unmerged branch, NOT this session's

- **What**: A local branch `feat/wx-examples-page` with one unmerged commit `41baff85` -- "feat(study): /reference/wx/products/<slug>/examples browsable catalog page (Drill Phase 2)". No PR.
- **Where**: branch `feat/wx-examples-page`, commit `41baff85`.
- **Why deferred**: Not created by this session -- ownership uncertain, so left untouched per wind-down safety rules. It belongs to a Drill Phase 2 effort, likely another session.
- **Next action**: the owning session should open a PR for it or clean it up. If abandoned, `git branch -D feat/wx-examples-page`.
- **Severity**: low

### Citation verification -- ongoing maintenance

- **What**: All 25 wx-product reference pages now carry zero `verified: false` citations (PR #1020), verified against AC 00-45H / FAA-H-8083-28B / AIM staged in the cache. This needs re-running whenever a source document gets a new edition.
- **Where**: `course/weather/references/products/*/page.md` frontmatter; gated by the `wx-citations` validator (PR #1008).
- **Why deferred**: not unfinished -- it is a recurring obligation. The monthly drift check (PR #1035) surfaces when a source doc changes; a re-verification pass follows.
- **Next action**: when `bun run sources report` flags a new edition of AC 00-45H / AIM / FAA-H-8083-28, re-run a citation verification pass against the new PDF.
- **Severity**: low

### faa-documentation-navigation course -- WP authored, not built

- **What**: The "Navigating FAA Documentation" course work package docs exist (PR #1022, merged -- spec/design/tasks/test-plan/OUT-OF-SCOPE) but the course itself is not built.
- **Where**: `docs/work-packages/faa-documentation-navigation/`.
- **Why deferred**: WP authoring and WP building are separate steps; building runs via `/ball-wp-build` as its own effort.
- **Next action**: run `/ball-wp-build faa-documentation-navigation` when ready to build the course.
- **Severity**: medium
