---
session: 9d89e07b-d64e-478c-bf06-b7658c4674b5
date: 2026-05-15
branch: main
type: unfinished
items: 2
---

# Unfinished work -- vitest isolation session

## Parallel-worker DB connection terminations in full vitest sweep

- **What**: `bun run vitest --run libs/bc/study/ scripts/db/` showed
  ~22 failures (of ~1307) with Postgres `FATAL 57P01 ProcessInterrupts`
  (`delete from "study"."review" where ...`). Connections are being
  killed mid-query. Most likely cause: vitest's parallel workers share
  the single `@ab/db/connection` pool against `airboss_unit_test`, and
  the per-run `globalSetup` (or a worker's own teardown) terminates
  connections other workers are still using. Not a regression from this
  session -- the 4 target test files all pass when run individually.
- **Where**: no artifact yet. Reproduce with
  `bun run vitest --run libs/bc/study/ scripts/db/`. Suspect interaction
  between `vitest.globalSetup.ts` connection-termination step and
  parallel worker pools.
- **Why deferred**: out of scope for the original ask (fix the 16
  failing tests + isolate the test DB). Surfaced during a wider
  verification sweep. User asked to wrap up.
- **Next action**: decide whether DB-touching test files should run
  single-threaded (vitest `poolOptions` / `fileParallelism: false` for
  the node project), or give each worker its own connection, or move to
  per-test transactional rollback. Confirm the failure is contention,
  not a real bug, by running the same file set with `--no-file-parallelism`.
- **Severity**: medium

## cleanup-cfr-part-14-orphan.ts is now dead weight on fresh DBs

- **What**: `scripts/db/cleanup-cfr-part-14-orphan.ts` was a one-shot
  repair for DBs seeded before the 14cfr14 root cause was fixed. With
  PR #986 the seed pipeline no longer creates the orphan, so the script
  is a no-op on every fresh DB. It is still useful for any long-lived
  DB that has the orphan baked in.
- **Where**: `scripts/db/cleanup-cfr-part-14-orphan.ts`
- **Why deferred**: deleting it now would strip the repair path for
  existing dev DBs that still carry the orphan. Per the project's
  "no legacy -- retire on sight" rule it should go eventually, but only
  after confirming no dev DB still needs it.
- **Next action**: once the team confirms all dev DBs have been
  reseeded (or run the cleanup once), delete the script and its test.
- **Severity**: low
