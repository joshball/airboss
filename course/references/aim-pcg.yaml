# Pilot/Controller Glossary (PCG).
#
# The AIM ships as a real reference via aim/<edition>/manifest.json (WP-AIM)
# and is no longer carried as a YAML placeholder here. The PCG remains as a
# placeholder until WP-PCG migrates it to its own manifest-driven corpus
# (per ADR 019 §1.2 the PCG is its own corpus distinct from the AIM).

references:
  - slug: pcg
    kind: pcg
    edition: current
    title: Pilot/Controller Glossary
    publisher: FAA
    url: https://www.faa.gov/air_traffic/publications/atpubs/pcg_html/
    subjects: [communications]
    primary_cert: null
